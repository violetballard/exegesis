Gate Results Summary (as of 2026-04-23)
-------------------------------------

Required automated gates reran against:

* current reviewed implementation head `adfa8cdadd43747ffbcb612e4151e262b13e52ca`
* reviewed implementation range `d7fd5d200358287fa42a18d39e2b277463b9b69f..adfa8cdadd43747ffbcb612e4151e262b13e52ca`
* current packet refresh workspace before this metadata-only fixer commit (`381e328a9cccc73cfb75ae299d2f4c2574d3403a`)

Gate status:

* `make scope-check` - passed
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Canonical demo-path step advanced: `retrieve relevant material` via the FTS-first engine surface with fail-closed excerpt lookup and auditable provenance.
Plan-alignment statement: this work advances `retrieve relevant material` by making the public excerpt lookup surface resolve through the authoritative SQLite FTS path, so PageIndex-only excerpt IDs fail closed under shared regression coverage and basket-promotion inputs stay deterministic.
AGENTS.md handoff completeness note: the reviewer-facing packet now explicitly carries the required canonical demo-path field for this narrowed retrieval slice.
Evidence note: `tests/unit/test_unified_retrieval.py` proves the narrowed contract on both surfaces used in re-review. `test_retrieval_service_rejects_pageindex_excerpt_payloads` covers `fetch_excerpt(...)` failing closed for PageIndex-only IDs, and `test_retrieve_fts_excerpt_returns_canonical_fts_payload` covers the canonical/public FTS excerpt helpers returning the same payload.
Reviewer fix closure: the authoritative handoff packet is retrieval-specific, remains classified as shared/high-risk work, and stays narrowed to reviewed commit `adfa8cdadd43747ffbcb612e4151e262b13e52ca`.
Scope note: this metadata-only handoff refresh documents an incremental retrieval-contract hardening slice for `retrieve relevant material`; it does not claim the full `feat-retrieval-fts` MVP or lane definition of done is complete.
Reviewer-facing truth sources: `THREAD_PACKET.md` and this gate summary carry the canonical demo-path mapping and plan-alignment wording for re-review on this branch. They intentionally describe only the narrowed FTS-only excerpt fail-closed slice and do not claim lane-level MVP completion.
Remaining risks: none identified in the reviewed slice beyond normal merge risk on a metadata-only packet refresh. Re-review should continue to anchor retrieval scope to `adfa8cdadd43747ffbcb612e4151e262b13e52ca` so later packet-refresh commits are not mistaken for new retrieval implementation changes.

Reviewed implementation files:

* `src/qual/retrieval/service.py`
* `tests/unit/test_unified_retrieval.py`

Metadata-only refresh files in this packet slice:

* `THREAD_PACKET.md`
* `docs/gate_passed.txt`

The authoritative handoff packet remains `THREAD_PACKET.md`.
