Gate Results Summary (as of 2026-04-24)
-------------------------------------

Feature lane: `codex/feat-retrieval-fts`
Packet role: `metadata-only reviewer-fix finalization`
Packet refresh commit corrected by this pass: `reported in final fixer handoff`
Reviewed implementation head: `adfa8cdadd43747ffbcb612e4151e262b13e52ca`
Reviewed implementation range: `378cf9a74a3658058079a32f186fcd254c4a4034..adfa8cdadd43747ffbcb612e4151e262b13e52ca`
Canonical demo-path step advanced: `retrieve relevant material`
Plan-alignment statement: This narrowed slice advances `retrieve relevant material` by making excerpt lookup fail closed to FTS-backed IDs and preserving deterministic provenance on the canonical retrieval path.
Basket-promotion statement: The deterministic and auditable excerpt contract gives downstream flows a stable payload they can safely promote or gather into the basket without PageIndex ambiguity.
Reviewer-fix reconciliation: the handoff packet explicitly names `retrieve relevant material`, adds the basket-promotion linkage, and keeps the reviewed slice narrowed to `378cf9a74a3658058079a32f186fcd254c4a4034..adfa8cdadd43747ffbcb612e4151e262b13e52ca`.
Packet refresh traceability: this fixer pass refreshed `THREAD_PACKET.md` and `docs/gate_passed.txt` without widening the reviewed implementation range.

Reviewed implementation scope:

* `fetch_excerpt` no longer falls back to PageIndex; excerpt lookup stays fail-closed on the canonical FTS path.
* Approved shared regression coverage in `tests/unit/test_unified_retrieval.py` proves PageIndex-only excerpt IDs now raise `KeyError`.
* This handoff remains intentionally limited to that reviewed slice and does not claim broader retrieval-lane or MVP completion.

Metadata-only packet refresh inventory for this fixer pass:

* `THREAD_PACKET.md`
* `docs/gate_passed.txt`

Packet mirror artifacts blocked in this environment:

* `.codex/kickoff_packets/feat-retrieval-fts.md`
* `.codex/lane_meta/feat-retrieval-fts.json`

AGENTS checkpoint evidence:

* `plan complete` - confirmed the reviewer fix scope: explicit canonical demo-path mapping, explicit basket-promotion linkage, and the narrowed reviewed implementation range.
* `before risky/shared file edit` - the packet now explicitly acknowledges approved shared regression coverage and the shared/high-risk budget posture.
* `first green tests` - all required gates passed after the packet refresh.
* `ready for handoff` - the writable handoff artifacts explicitly map the narrowed slice to `retrieve relevant material` and explain how the deterministic excerpt contract supports basket promotion.

Verification:

* `make scope-check` - passed
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Roadmap items affected:

* `Milestone 3: Real workflow loop`
* `feat-retrieval-fts`: authoritative FTS-first retrieval feeding the engine loop

Vision capabilities affected:

* `Retrieval-first context handling`
* `Auditable state and workflow`

Routing/provider impact:

* none

Risks / blockers:

* Residual risk: this handoff updated shared regression coverage in `tests/unit/test_unified_retrieval.py`, so any later retrieval change that intentionally broadens excerpt lookup semantics or reintroduces PageIndex fallback will need coordinated updates to that shared test surface.
* Blockers: `.codex/kickoff_packets/feat-retrieval-fts.md` and `.codex/lane_meta/feat-retrieval-fts.json` could not be refreshed with `apply_patch` in this environment.
