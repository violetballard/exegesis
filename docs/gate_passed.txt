Gate Results Summary (as of 2026-04-23)
-------------------------------------

Required automated gates reran for the narrowed metadata-only packet refresh on:

* current submitted tip before this packet refresh commit `e96b9e841bca7609b37a168126eb982e8f352f49`
* reviewed implementation head `adfa8cdadd43747ffbcb612e4151e262b13e52ca`
* reviewed implementation range `d7fd5d200358287fa42a18d39e2b277463b9b69f..adfa8cdadd43747ffbcb612e4151e262b13e52ca`
* reviewed implementation files `src/qual/retrieval/service.py` and `tests/unit/test_unified_retrieval.py`
* refreshed reviewer-facing packet files `THREAD_PACKET.md` and `docs/gate_passed.txt`
* stale `.codex/` packet mirrors not refreshed due to local filesystem write restrictions `.codex/kickoff_packets/feat-retrieval-fts.md` and `.codex/lane_meta/feat-retrieval-fts.json`
* shared regression approval basis `tests/unit/test_unified_retrieval.py`

Gate status:

* `make scope-check` - passed (`no policy for branch 'codex/feat-retrieval-fts'; skipping`)
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Focused retrieval proof:

* `python3 -m unittest tests.unit.test_unified_retrieval.UnifiedRetrievalTests.test_fetch_excerpt_requires_an_fts_lookup_hit` - passed
* `python3 -m unittest tests.unit.test_unified_retrieval.UnifiedRetrievalTests.test_retrieve_auto_provenance_bundle_matches_result_snapshot` - passed
* `python3 -m unittest tests.unit.test_unified_retrieval.UnifiedRetrievalTests.test_retrieve_fts_source_bundle_matches_result_snapshot` - passed
* `python3 -m unittest tests.unit.test_unified_retrieval` - passed (`214 tests`)

Canonical demo-path step advanced: `retrieve relevant material`.
Plan-alignment statement: this reviewed retrieval slice makes `retrieve relevant material` more real by keeping runtime retrieval hits and doc hits FTS-only, exposing deterministic provenance/source bundles through the public retrieval helpers, and forcing excerpt lookup to fail closed unless the hit comes from the authoritative SQLite FTS path.
Scope note: this packet documents the real reviewed retrieval/runtime surface already present in `d7fd5d20..adfa8cda`; it does not claim any new routing or provider behavior beyond that reviewed range.
Budget compliance statement: this packet is shared/high-risk work because it includes the approved shared regression surface `tests/unit/test_unified_retrieval.py`, and that shared file covers fail-closed excerpt lookup plus provenance, payload, and helper-surface behavior, so the handoff is read against the 4-task cap rather than the low-risk 8-task cap.
Shared approval note: the approved `tests/unit/test_unified_retrieval.py` coverage now explicitly includes the fail-closed excerpt regression plus the payload, provenance-bundle, and public helper assertions exercised by the narrowed reviewed range.
Traceability statement: re-review should validate the narrowed reviewed implementation range `d7fd5d20..adfa8cda`; the current packet refresh commit is metadata-only and does not broaden retrieval scope beyond that range.

The authoritative handoff packet remains `THREAD_PACKET.md`.
