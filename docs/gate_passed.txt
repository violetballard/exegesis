Gate Results Summary (as of 2026-04-23)
-------------------------------------

Required automated gates reran against the current handoff refresh worktree content for:

* current branch tip before this fixer refresh `422d1fdabf53f56e70a26eb00fce31a15bc189f7`
* reviewed implementation head `adfa8cdadd43747ffbcb612e4151e262b13e52ca`
* reviewed implementation range `d7fd5d200358287fa42a18d39e2b277463b9b69f..adfa8cdadd43747ffbcb612e4151e262b13e52ca`
* metadata-only packet refresh files `.codex/kickoff_packets/feat-retrieval-fts.md`, `.codex/lane_meta/feat-retrieval-fts.json`, `THREAD_PACKET.md`, and `docs/gate_passed.txt`

Gate status:

* `make scope-check` - passed (`no policy for branch 'codex/feat-retrieval-fts'; skipping`)
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Canonical demo-path step advanced: `retrieve relevant material`, via the FTS-only excerpt fail-closed contract.
Plan-alignment statement: this branch makes Milestone 3's `retrieve relevant material` step more real by forcing excerpt lookup through the canonical SQLite FTS path and rejecting PageIndex-only excerpt IDs so downstream basket-promotion inputs stay deterministic and auditable.
Scope-tightening statement: the reviewed runtime slice stays inside the current Milestone 3 FTS-first retrieval MVP path because it removes the PageIndex fallback from excerpt lookup without restoring PageIndex or embeddings as required retrieval paths.
Metadata-only commits after the reviewed implementation head and before this fixer refresh: `31b91107`, `94a1fb6b`, `e1530c68`, and `422d1fda`.
Metadata mirror note: `.codex/kickoff_packets/feat-retrieval-fts.md` and `.codex/lane_meta/feat-retrieval-fts.json` remain stale in this worktree because writes there fail with `Operation not permitted`; `THREAD_PACKET.md` and this file are the reviewer-facing source of truth for this refresh.
Reviewer-fix note: this pass revalidated the narrowed `adfa8cda` implementation slice after the reviewer asked for an explicit `retrieve relevant material` handoff statement and tighter retrieval-only scope wording.

Reviewed implementation files:

* `src/qual/retrieval/service.py`
* `tests/unit/test_unified_retrieval.py`

Metadata-only refresh files in this fixer slice:

* `.codex/kickoff_packets/feat-retrieval-fts.md`
* `.codex/lane_meta/feat-retrieval-fts.json`
* `THREAD_PACKET.md`
* `docs/gate_passed.txt`

The authoritative handoff packet remains `THREAD_PACKET.md`.
