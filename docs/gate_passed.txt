Gate Results Summary (as of 2026-04-24)
-------------------------------------

Feature lane: `codex/feat-retrieval-fts`
Packet role: `packet traceability correction for the actual branch tip`
Packet refresh commit corrected by this pass: `d4ce75fdeb2e2f4dbf1dcebd06a3e53659db8165`
Reviewed implementation head: `6e963e1b209cec127e7da6734aa5ca3328a05f88`
Reviewed implementation range: `75572c120239a84402a82b845c3df797806fcdf4..6e963e1b209cec127e7da6734aa5ca3328a05f88`
Canonical demo-path step advanced: `retrieve relevant material`
Plan-alignment statement: In the Milestone 3 engine-first demo path, this reviewed range advances `retrieve relevant material` by keeping excerpt lookup on the canonical FTS-backed path and making retrieval payload, provenance, policy snapshot, and missing-query-context audit behavior deterministic and reviewable.
Reviewer-fix reconciliation: the handoff packet is regenerated against the real implementation head `6e963e1b209cec127e7da6734aa5ca3328a05f88`, includes the in-scope retrieval commits through that head, and keeps the repo-visible handoff artifacts aligned on that actual reviewed range.
Packet refresh traceability: `d4ce75fdeb2e2f4dbf1dcebd06a3e53659db8165` refreshed `THREAD_PACKET.md`; this pass refreshes `THREAD_PACKET.md` and `docs/gate_passed.txt` to agree on the actual reviewed implementation head. The mirrored `.codex` packet files require a separate write path in this sandbox.
Packet refresh traceability correction: commit `6e963e1b209cec127e7da6734aa5ca3328a05f88` is reviewed implementation scope, not a metadata-only packet refresh.

Reviewed implementation scope:

* `fetch_excerpt` no longer falls back to PageIndex; excerpt lookup stays fail-closed on the canonical FTS path.
* Approved shared regression coverage in `tests/unit/test_unified_retrieval.py` proves missing excerpt query context is reported with `lookup_query_context_status: "missing"` and PageIndex-only excerpt IDs still fail closed.
* Retrieval policy snapshots are deep-copied before response enrichment so packet- or consumer-side mutation cannot alias back into stored retrieval metadata.

Packet artifacts refreshed by this fixer pass:

* `THREAD_PACKET.md`
* `docs/gate_passed.txt`

AGENTS checkpoint evidence:

* `plan complete` - regenerated the reviewed implementation range to the real non-metadata tip and mapped it explicitly to `retrieve relevant material`.
* `before risky/shared file edit` - the packet explicitly acknowledges approved shared regression coverage and the shared/high-risk budget posture.
* `first green tests` - all required gates passed after the branch-tip packet restamp.
* `ready for handoff` - the writable handoff artifacts explicitly map the reviewed range to `retrieve relevant material` and describe the real implementation files in scope.

Verification:

* `make scope-check` - passed
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Roadmap items affected:

* `Milestone 3: Real workflow loop`
* `feat-retrieval-fts`: retrieval/search

Vision capabilities affected:

* `Retrieval-first context handling`
* `Auditable state and workflow`

Routing/provider impact:

* none

Risks / blockers:

* Residual risk: this handoff still depends on shared regression coverage in `tests/unit/test_unified_retrieval.py`, so any later retrieval change that broadens excerpt lookup semantics or alters the retrieval audit payload shape will need coordinated updates to that shared test surface.
* Blockers: mirrored packet files under `.codex/` could not be updated with `apply_patch` in this sandbox even though the repo-visible handoff artifacts were refreshed successfully.
