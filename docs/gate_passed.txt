Gate Results Summary (as of 2026-04-24)
-------------------------------------

Feature lane: `codex/feat-retrieval-fts`
Packet role: `reviewer-fix demo-path alignment`
Reviewed implementation head: `adfa8cdadd43747ffbcb612e4151e262b13e52ca`
Reviewed implementation scope: `FTS-first excerpt lookup hardening in the accepted retrieval slice`
Canonical demo-path step advanced: `retrieve relevant material`
Plan-alignment statement: This change makes `retrieve relevant material` more real by ensuring excerpt lookup resolves only through the canonical SQLite FTS path, so downstream basket/workflow consumers cannot silently fall back to PageIndex-only IDs.
Reviewer-fix reconciliation: the handoff is intentionally narrowed to the accepted `adfa8cdadd43747ffbcb612e4151e262b13e52ca` slice, keeps retrieval FTS-first for the MVP, and carries the required canonical demo-path statement in the packet itself.

Reviewed implementation scope:

* The accepted retrieval slice keeps excerpt lookup on the canonical SQLite FTS path for downstream engine and basket consumers.
* The accepted slice includes approved shared regression coverage in `tests/unit/test_unified_retrieval.py`, so the handoff remains shared/high-risk under `AGENTS.md`.

AGENTS checkpoint evidence:

* `plan complete` - the handoff was narrowed to the accepted `adfa8cdadd43747ffbcb612e4151e262b13e52ca` slice and the explicit demo-path mapping was added to the packet.
* `before risky/shared file edit` - the shared/high-risk boundary was restated because the accepted slice includes approved shared regression coverage in `tests/unit/test_unified_retrieval.py`.
* `first green tests` - all required gates passed after the packet refresh.
* `ready for handoff` - the writable handoff artifacts explicitly name `retrieve relevant material` and explain the FTS-only excerpt lookup impact in the handoff itself.

Verification:

* `make scope-check` - passed
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Roadmap items affected:

* `Milestone 3: Real workflow loop`
* `feat-retrieval-fts`: retrieval/search

Vision capabilities affected:

* `Retrieval-first context handling`
* `Auditable state and workflow`

Routing/provider impact:

* none
