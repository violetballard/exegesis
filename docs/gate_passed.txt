Gate Results Summary (as of 2026-04-24)
-------------------------------------

Feature lane: `codex/feat-retrieval-fts`
Packet role: `metadata-only reviewer-fix finalization`
Packet refresh trace anchor before this fixer attempt: `65199b1a7bf57dd9a2c2fc15ba29c09402a7e7ff`
Reviewed implementation head: `adfa8cdadd43747ffbcb612e4151e262b13e52ca`
Reviewed implementation range: `378cf9a74a3658058079a32f186fcd254c4a4034..adfa8cdadd43747ffbcb612e4151e262b13e52ca`
Canonical demo-path step advanced: `retrieve relevant material`
Plan-alignment statement: this reviewed slice strengthens `retrieve relevant material` by keeping retrieval/search FTS-first and forcing public excerpt lookup to stay on the authoritative FTS path.
Explicit Milestone 3 mapping: this slice advances `Milestone 3: Real workflow loop` by keeping retrieval/search FTS-first and structured.
FTS-first confirmation: SQLite FTS remains authoritative for the MVP in the reviewed range; PageIndex and embeddings remain deferred or compatibility paths and are not required runtime fallbacks for the public excerpt lookup surface.

Reviewed implementation scope:

* SQLite FTS remains authoritative for the reviewed slice.
* `fetch_excerpt` stays on the canonical FTS-only path in `src/qual/retrieval/service.py`.
* Approved shared regression coverage proves that PageIndex-only excerpt ids fail closed with `KeyError`.
* The reviewed implementation scope remains limited to `src/qual/retrieval/service.py` and `tests/unit/test_unified_retrieval.py`.

Writable packet artifacts refreshed in this fixer pass:

* `THREAD_PACKET.md`
* `docs/gate_passed.txt`

Read-only packet artifacts blocked in this sandbox:

* `.codex/kickoff_packets/feat-retrieval-fts.md`
* `.codex/lane_meta/feat-retrieval-fts.json`

AGENTS checkpoint evidence:

* `plan complete` - the high-risk kickoff was locked to the narrowed reviewed implementation range `378cf9a74a3658058079a32f186fcd254c4a4034..adfa8cdadd43747ffbcb612e4151e262b13e52ca` and the canonical demo-path step `retrieve relevant material`.
* `first green tests` - all required gates passed on the packet-refresh branch head before this re-review handoff was finalized.
* `before risky/shared file edit` - the handoff stayed metadata-only, and the shared/high-risk boundary was called out because the reviewed scope still includes approved shared regression coverage in `tests/unit/test_unified_retrieval.py`.
* `ready for handoff` - the top-level packet and gate summary now carry the reviewed implementation head, narrowed reviewed range, demo-path mapping, and checkpoint trail required for re-review; the `.codex` packet mirrors remain read-only in this sandbox and are called out separately.

Verification:

* `make scope-check` - passed
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Roadmap items affected:

* `Milestone 3: Real workflow loop`
* `feat-retrieval-fts`: retrieval/search

Vision capabilities affected:

* `Retrieval-first context handling`
* `Auditable state and workflow`

Routing/provider impact:

* none
