Gate Results Summary (as of 2026-04-24)
-------------------------------------

Feature lane: `codex/feat-retrieval-fts`
Reviewed implementation head: `4ec62ffecef5ee266d766cbb35ffc531cd597e60`
Reviewed implementation range: `adfa8cdadd43747ffbcb612e4151e262b13e52ca..4ec62ffecef5ee266d766cbb35ffc531cd597e60`
Canonical demo-path step advanced: `retrieve relevant material`
Scope: fail closed on unsupported scoped-query payloads in the canonical FTS strategy and preserve approved shared regression coverage proving unsupported scoped queries do not execute as canonical FTS requests.
Current branch tip before this handoff refresh commit: `91017271631061ff5c6d76d32ac07bb168f21c28`

Reviewed implementation files:

* `src/qual/engine/retrieval/fts_strategy.py`
* `tests/unit/test_unified_retrieval.py`

Visible handoff artifacts refreshed in this fixer pass:

* `THREAD_PACKET.md`
* `docs/gate_passed.txt`

Plan-alignment note:

* This slice advances `retrieve relevant material` by forcing excerpt lookup and scoped-query handling to resolve only through supported canonical FTS requests, so unsupported scoped queries fail closed on the retrieval step itself.
* Direct handoff statement: this handoff advances the canonical demo-path step `retrieve relevant material` by keeping the engine retrieval surface on supported SQLite FTS requests and by keeping approved shared regression coverage proving unsupported scoped queries are rejected.

Budget classification:

* Shared/high-risk because `tests/unit/test_unified_retrieval.py` is a shared-by-approval file for this lane.
* The narrowed reviewed implementation commit changes `2` files with `36` lines touched, which stays within the high-risk budget limits.
* Later packet-refresh commits after `4ec62ffecef5ee266d766cbb35ffc531cd597e60` remain outside this reviewed slice unless a new packet explicitly regenerates the reviewed implementation range.

Verification:

* `make scope-check` - passed
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Roadmap items affected:

* `Milestone 3: Real workflow loop`
* `feat-retrieval-fts` as a narrow scoped-query fail-closed slice rather than as lane completion

Vision capabilities affected:

* `2. Retrieval-first context handling`
* `6. Auditable state and workflow`

Routing/provider impact:

* none

Remaining blocker:

* The sandbox rejects writes under `.codex/`, so `.codex/kickoff_packets/feat-retrieval-fts.md` and `.codex/lane_meta/feat-retrieval-fts.json` remain stale and could not be corrected in this fixer pass.
