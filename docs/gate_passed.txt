Gate Results Summary (as of 2026-04-23)
-------------------------------------

Required automated gates reran against the current handoff refresh worktree content for:

* current branch tip before this fixer refresh `e8a8717b138f7d3551cd89bc3dc68071d5dad5f3`
* reviewed implementation head `a9eaaaa79afcd57cde90738adee76e52cfc29adb`
* reviewed implementation range `d7fd5d200358287fa42a18d39e2b277463b9b69f..a9eaaaa79afcd57cde90738adee76e52cfc29adb`
* metadata-only packet refresh files `THREAD_PACKET.md` and `docs/gate_passed.txt`

Gate status:

* `make scope-check` - passed (`no policy for branch 'codex/feat-retrieval-fts'; skipping`)
* `./quality-format.sh --check` - passed
* `./quality-lint.sh` - passed
* `./quality-test.sh` - passed
* `./typecheck-test.sh` - passed
* `make ci` - passed

Canonical demo-path step advanced: `retrieve relevant material`, via the FTS-only excerpt fail-closed contract.
Shared-file approval basis: `tests/unit/test_unified_retrieval.py` is the only non-owned edit in the reviewed slice, and this handoff treats it as the explicit shared-by-approval regression exception under `THREAD_OWNERSHIP.md`'s shared-file mechanism (`SCOPE_ALLOW_SHARED=1 make scope-check`) for canonical retrieval-contract coverage.
Plan-alignment statement: this branch makes Milestone 3's `retrieve relevant material` step more real by forcing excerpt lookup through the canonical SQLite FTS path and rejecting PageIndex-only excerpt IDs, hardening the retrieval surface used before basket promotion and downstream workflow use.
Scope-tightening statement: the reviewed runtime slice stays inside the current Milestone 3 FTS-first retrieval MVP path because it removes the PageIndex fallback from excerpt lookup without restoring PageIndex or embeddings as required retrieval paths.
Metadata-only commit/file accounting after the reviewed implementation head and before this fixer refresh:
* `94a1fb6b` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
* `e1530c68` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
* `422d1fda` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
* `02f89443` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
* `08cd31e3` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
* `ef827a8a` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
* `e013b29a` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
* `61b9c36f` -> `THREAD_PACKET.md`, `docs/gate_passed.txt`
Reviewer-fix note: this pass revalidated the truthful reviewed implementation slice after the reviewer asked for an explicit canonical demo-path statement and direct task-to-step linkage for the basket-promotion-facing retrieval surface.
Task-to-step mapping note: removing the PageIndex excerpt fallback and locking in the shared `KeyError` regression both advance `retrieve relevant material` by hardening the canonical FTS retrieval surface that feeds basket promotion and downstream workflow use.

Reviewed implementation files:

* `src/qual/engine/retrieval/payload.py`
* `src/qual/retrieval/service.py`
* `tests/unit/test_unified_retrieval.py`

Metadata-only refresh files in this fixer slice:

* `THREAD_PACKET.md`
* `docs/gate_passed.txt`

The authoritative handoff packet remains `THREAD_PACKET.md`.
