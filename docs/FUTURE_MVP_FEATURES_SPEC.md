# Future Summer MVP Feature Specs

This document defines disabled future work for the real summer MVP. It is specification scaffolding only:
no runtime editor basics, qualitative coding, citation, export, Zotero import, or formatting-bar behavior should be active until the relevant lane is explicitly enabled.

Implementation rule:
- Each milestone below is written so its lane can be enabled in batches later without another planning pass.
- Each lane should still create its own kickoff packet and stop at normal review/integration gates.
- Do not activate these lanes until Milestones 1-5 are standing and the engine/client contract is ready for the relevant feature.

## Shared Conventions For Milestones 9-14

Project entities:
- Stable IDs should be opaque strings generated by storage, not derived from names.
- Human names should remain editable without breaking references.
- User-visible commands must be registered in the command palette and, where specified, in the top shortcut rows.
- Textual/browser shell mock paths may use fixture-backed state first, but engine-facing contracts must avoid mock-only names.

Document offsets:
- Any feature that stores a range in a document should store `document_id`, `start_offset`, `end_offset`, `selected_text`, and `document_content_hash`.
- If the document hash no longer matches on reload, show a recoverable stale-range state instead of silently applying edits to the wrong text.

Shortcut rows:
- Existing top rows remain grouped by workflow area.
- New feature rows should be added only when the corresponding feature lane is enabled.
- Shortcut labels must match command-palette command names closely enough that users can discover the same action in either place.

## Milestone 9: Basic Qualitative Coding

Lane: `feat-qual-coding` (disabled)

Intent:
- Add first-class qualitative codes to project state and storage.
- Let selected document text receive one simple code with a color highlight.
- Add project-browser organization for document folders and one-level parent codes.
- Provide enough inspector/code-view detail that coding feels like real qualitative analysis, not a decorative highlight.

### Data Model

Add these engine-level concepts:
- `Code`
  - `id`: stable ID
  - `project_id`
  - `name`
  - `description`: optional text
  - `color`: stable highlight color token or hex value
  - `parent_code_id`: optional; only one parent level for MVP
  - `sort_order`
  - `created_at`, `updated_at`
- `CodeAssignment`
  - `id`
  - `project_id`
  - `document_id`
  - `document_type`: draft, memo, summary, transcript, or literature
  - `code_id`
  - `start_offset`, `end_offset`
  - `selected_text`
  - `document_content_hash`
  - `created_at`, `updated_at`
- `ProjectFolder`
  - `id`
  - `project_id`
  - `section`: drafts, memos, summaries, transcripts, literature, or codes
  - `name`
  - `parent_folder_id`: optional for document sections; keep nesting shallow unless explicitly enabled later
  - `sort_order`
  - `created_at`, `updated_at`
- Document metadata additions:
  - `folder_id`: optional folder assignment for document sections
- Code metadata additions:
  - parent code is represented by `Code.parent_code_id`; creating a folder under Codes creates a parent code, not a generic folder.

Storage constraints:
- A child code may have at most one parent code.
- A parent code may not be nested under another parent code in MVP.
- A `CodeAssignment` must reference an existing code and document.
- Deleting a code requires either deleting its assignments or moving them to another code through a confirmation flow; default MVP behavior is confirmation plus assignment deletion.

### Engine/API Surface

Add service actions:
- `create_code(project_id, name, color?, parent_code_id?) -> Code`
- `rename_code(code_id, name) -> Code`
- `delete_code(code_id, delete_assignments: bool) -> DeleteResult`
- `move_code(code_id, parent_code_id?, sort_order?) -> Code`
- `create_project_folder(project_id, section, name, parent_folder_id?) -> ProjectFolder`
- `rename_project_folder(folder_id, name) -> ProjectFolder`
- `delete_project_folder(folder_id, move_children_to_parent: bool) -> DeleteResult`
- `move_document_to_folder(document_id, folder_id?, sort_order?) -> DocumentRef`
- `apply_code_to_selection(document_id, code_id, selection_range) -> CodeAssignment`
- `delete_code_assignment(assignment_id) -> DeleteResult`
- `list_code_appearances(code_id) -> list[CodeAppearance]`
- `get_code_summary(code_id) -> CodeSummary`

Return shape for `CodeAppearance`:
- assignment ID
- document ID, title, type
- excerpt around coded range
- start/end offsets
- document content hash

Return shape for `CodeSummary`:
- code ID, name, description, color
- parent code, if any
- child code list
- assignment count
- document count
- appearances grouped by document

### UI And Commands

Project browser:
- Add `New Folder` action.
- If selected item is a document section or folder, `New Folder` creates a `ProjectFolder` in that document section.
- If selected item is Codes or a parent code area, `New Folder` creates a parent `Code`.
- Dragging a document into a folder updates `document.folder_id`.
- Dragging a code under another code sets `parent_code_id`, if it does not violate one-level nesting.
- Dragging a child code back to Codes clears `parent_code_id`.

Document pane:
- Selecting text and choosing `Add Code` opens a small centered modal or palette picker for existing code/new code.
- Applying a code highlights the selected range with the code color.
- MVP supports one code per exact selected range. If the user codes overlapping text, create a separate assignment but keep rendering deterministic by nearest/smallest assignment first; fuller overlap tooling waits.

Inspector:
- When selected text has a code assignment, show:
  - code name and color
  - parent code if present
  - child code count if selected code is a parent
  - frequency count across project
  - document count
  - document list of appearances with title/type and short excerpt
- Appearance rows are clickable and open the relevant document at the coded range.

Code-focused document view:
- When a code is selected in the project browser, document pane switches to code view.
- Code view shows:
  - title and description
  - parent/child info
  - total assignment count and document count
  - grouped document list
  - excerpts for each appearance
  - click-to-open document at excerpt

Shortcut row and palette:
- Add a dedicated coding row when lane is enabled.
- Required commands:
  - `Add Code`
  - `Delete Code`
  - `New Folder`
  - `Move Code Up`
  - `Move Code Down`
  - `Open Code View`

### Implementation Batches

1. Storage/model contract
   - Add code, assignment, and folder models.
   - Add persistence and serialization tests.
2. Engine actions
   - Implement create/rename/delete/move code and folder actions.
   - Implement apply/delete assignment and appearance queries.
3. Project browser integration
   - Add folders and code hierarchy rendering.
   - Add drag/drop constraints and command-palette actions.
4. Document highlight integration
   - Render code highlights over document ranges.
   - Handle stale ranges and overlapping ranges predictably.
5. Inspector and code view
   - Add coded-selection inspector details.
   - Add code-focused document view and clickable appearances.

### Test Plan

- Creating a document folder under Drafts nests only draft documents.
- Creating `New Folder` under Codes creates a parent code.
- Dragging a child code under a parent succeeds.
- Dragging a parent code under another parent fails with a clear UI message.
- Applying a code to selected text creates a `CodeAssignment` with stable offsets and selected text.
- Deleting a code prompts for assignment deletion and removes highlights after confirmation.
- Inspector shows code name, parent/child info, frequencies, and document appearances.
- Clicking a code appearance opens the target document and scrolls to the coded range.
- Selecting a code in the project browser opens the code-focused document view.
- Command palette contains all coding commands.

## Milestone 10: Editor Basics

Lane: `feat-editor-basics` (disabled)

Intent:
- Add expected text-editor primitives after the document pane has stable selection and edit APIs.
- Keep copy/paste/undo/redo separate from coding/project taxonomy behavior.

### Data And State

Document editor state additions:
- `selection_range`: current selected text range, if any
- `cursor_offset`: insertion point when no selection exists
- `undo_stack`: bounded stack of document edit operations
- `redo_stack`: bounded stack of reverted document edit operations

Edit operation shape:
- `operation_id`
- `document_id`
- `before_hash`
- `after_hash`
- `start_offset`, `end_offset`
- `inserted_text`
- `removed_text`
- `timestamp`
- `source`: typing, paste, draft, rewrite_apply, formatting, citation, or coding

MVP stack limits:
- Keep at least 100 edit operations per open document.
- Clear redo stack whenever a new edit happens after undo.
- Persist undo/redo only for the active session unless later persistence is explicitly enabled.

### Engine/API Surface

Add editor actions:
- `copy_selection(document_id, selection_range) -> ClipboardPayload`
- `paste_text(document_id, cursor_or_selection, text) -> EditResult`
- `undo_document_edit(document_id) -> EditResult | NoOp`
- `redo_document_edit(document_id) -> EditResult | NoOp`
- `record_document_edit(document_id, edit_operation) -> None`

Clipboard payload:
- plain text is required
- markdown text is the same as plain text for MVP
- rich clipboard formats are out of scope

### UI And Commands

Document pane:
- `Copy` requires a non-empty selection.
- `Paste` inserts at cursor or replaces selected text.
- `Undo` reverts the most recent operation for the active document.
- `Redo` reapplies the most recent undone operation.
- If no action is available, disable the command and show a small status message.

Shortcut row and palette:
- Add a dedicated editor basics row when lane is enabled.
- Required commands:
  - `Copy`
  - `Paste`
  - `Undo`
  - `Redo`

Behavior with generated edits:
- Draft insertion, rewrite apply, citation insertion, and formatting changes should record undoable edit operations once those lanes exist.
- Undo must restore the document text and cursor/selection when possible.

### Implementation Batches

1. Editor operation model
   - Add edit operation representation and stack behavior.
2. Document pane API
   - Expose copy/paste/undo/redo through stable document pane methods.
3. Clipboard integration
   - Wire system clipboard where available, with mock/test clipboard fallback.
4. Command/shortcut integration
   - Add top row and palette commands.
5. Generated edit integration
   - Make existing draft/rewrite paths record undoable operations once lane is enabled.

### Test Plan

- Copy with selected text returns exact selected Markdown text.
- Copy with no selection is disabled or no-op with clear status.
- Paste with cursor inserts text at cursor.
- Paste with selection replaces selected text.
- Undo after paste restores previous document text.
- Redo after undo restores pasted text.
- New edit after undo clears redo stack.
- Undo stack is per document, not global across all documents.
- Command palette contains Copy, Paste, Undo, Redo.

## Milestone 11: Citation Support

Lane: `feat-citations` (disabled)

Intent:
- Let drafts cite saved literature manually and preserve model-used literature citations.
- Store citations in Pandoc-compatible Markdown while rendering them as clickable literature links in the document pane.

### Data Model

Add citation concepts:
- `Citation`
  - `id`
  - `project_id`
  - `document_id`
  - `literature_id`
  - `citation_key`
  - `locator`: optional page number or other locator
  - `prefix`: optional Pandoc citation prefix
  - `suffix`: optional Pandoc citation suffix
  - `start_offset`, `end_offset`
  - `document_content_hash`
  - `created_at`, `updated_at`
- Literature metadata must expose or derive:
  - `citation_key`
  - title
  - authors
  - year/date
  - DOI/URL where available

Pandoc backing syntax:
- Use citation keys of the form `@citation_key`.
- Store page/locator data in Pandoc-compatible citation syntax when practical.
- Rendered document view may display the citation as a clickable compact label while source Markdown remains Pandoc-compatible.

### Engine/API Surface

Add citation actions:
- `list_citable_literature(project_id) -> list[LiteratureCitationOption]`
- `insert_citation(document_id, literature_id, cursor_or_selection, locator?, prefix?, suffix?) -> CitationInsertResult`
- `list_document_citations(document_id) -> list[Citation]`
- `resolve_citation_link(citation_id) -> LiteratureRef`
- `extract_citations_from_markdown(document_id) -> list[Citation]`

LLM integration contract:
- Any draft/rewrite/chat mode that uses literature context must receive literature metadata with citation keys.
- Draft/rewrite output that references literature should include citation keys in text.
- If generated output cites unknown keys, surface a review warning instead of silently accepting.

### UI And Commands

Document top row:
- Place `Cite` next to basket commands and before `Export`.
- `Cite` opens a centered modal/palette for saved literature.
- User can select literature and optionally enter page number/locator.

Document pane rendering:
- Backing Markdown remains Pandoc-compatible.
- Render citation markers as clickable links to the literature item.
- Clicking a citation opens the cited literature document/metadata in the document pane or inspector.

Inspector:
- When cursor/selection is on a citation, show literature title, authors, year, DOI/URL, and locator.
- Provide edit/remove citation actions later if not implemented in the first batch.

Shortcut row and palette:
- Required commands:
  - `Cite Literature`
  - `Open Citation`
  - `Remove Citation`

### Implementation Batches

1. Citation key and metadata contract
   - Ensure literature records can provide citation keys.
2. Manual insertion
   - Add insert citation action, modal, and Pandoc-compatible Markdown insertion.
3. Link rendering
   - Render citations as clickable links in document view.
4. Inspector support
   - Show citation details and open/remove actions.
5. LLM citation requirements
   - Pass citation keys into model context and validate generated keys.

### Test Plan

- Literature records expose stable citation keys.
- Manual citation insertion writes Pandoc-compatible Markdown.
- Optional locator/page number is preserved.
- Citation renders as a clickable link in document pane.
- Clicking citation opens the cited literature item.
- Inspector shows citation metadata when citation is selected.
- LLM-generated output with known literature citation keys is accepted.
- LLM-generated output with unknown citation keys creates a clear warning.
- Command palette contains citation commands.

References:
- [Pandoc citations](https://pandoc.org/MANUAL.html?citationMarker=43dcd9a7-70db-4a1f-b0ae-981daa162054)

## Milestone 12: Export Support

Lane: `feat-export` (disabled)

Intent:
- Export drafts as raw Markdown, APA PDF, or APA DOCX.
- Use Pandoc/CSL-compatible source data so APA works first and future styles can be added without redesign.

### Data Model

Draft metadata additions:
- `author_name`
- `institution`
- `course_or_project`: optional
- `date`: optional; default export date if blank
- `title_override`: optional; defaults to document title
- `citation_style`: default `apa`

Export request:
- `document_id`
- `format`: markdown, pdf, or docx
- `citation_style`: default `apa`
- `identity_metadata`
- `include_reference_list`: default true for PDF/DOCX, false for raw Markdown unless selected
- `output_path`: optional; otherwise save under project exports

Export result:
- `export_id`
- `document_id`
- `format`
- `output_path`
- `warnings`
- `created_at`

### Engine/API Surface

Add export actions:
- `get_export_metadata(document_id) -> ExportMetadata`
- `update_export_metadata(document_id, metadata) -> ExportMetadata`
- `preview_export_request(document_id, format, style) -> ExportPreview`
- `export_document(request) -> ExportResult`
- `list_export_formats() -> list[ExportFormat]`

Pandoc/CSL contract:
- Raw Markdown export emits the backing document text plus optional frontmatter.
- PDF/DOCX export runs through Pandoc-compatible Markdown and bibliography data.
- APA is the first supported style.
- Future MLA/Chicago/university styles are added by registering CSL/style config, not changing document citation syntax.

### UI And Commands

Document top row:
- Add `Export` after `Cite`.
- Export opens a centered modal.

Export modal:
- Format options: raw Markdown, PDF, DOCX.
- Citation style: APA only enabled for MVP; future styles visible only if implemented.
- Editable identity fields: author name, institution, optional course/project, optional date, title override.
- Confirmation copy explains that citation/reference formatting depends on literature metadata.

Inspector:
- When a draft is open, show export/APA metadata fields as editable document metadata.
- On draft create/import, prompt for author/institution metadata or allow blank with later reminder.

Shortcut row and palette:
- Required commands:
  - `Export Document`
  - `Export Markdown`
  - `Export PDF`
  - `Export DOCX`

### Implementation Batches

1. Export metadata model
   - Add draft identity metadata and inspector editing contract.
2. Raw Markdown export
   - Write source Markdown with stable filename and export result record.
3. Pandoc export adapter
   - Add PDF/DOCX adapter with APA style and reference list.
4. Export modal
   - Add format choices and editable identity metadata confirmation.
5. Future style registry
   - Add style registry scaffolding for CSL-based formats.

### Test Plan

- Draft create/import can store author and institution metadata.
- Inspector edits export metadata and persists it.
- Raw Markdown export preserves backing Markdown text.
- APA PDF export includes title/author/institution and reference list.
- APA DOCX export includes title/author/institution and reference list.
- Missing required identity metadata produces editable warning, not a crash.
- Missing citation metadata produces export warnings.
- Future style registry can list APA without enabling unsupported styles.
- Command palette contains export commands.

References:
- [Pandoc citations](https://pandoc.org/MANUAL.html?citationMarker=43dcd9a7-70db-4a1f-b0ae-981daa162054)

## Milestone 13: Zotero Import

Lane: `feat-zotero-import` (disabled)

Intent:
- Add Zotero as a literature import source without bypassing the normal literature/OCR pipeline.
- Import metadata first, then import attached files through the existing literature/OCR flow.

### Data Model

Zotero account state:
- `account_id`
- `user_id` or Zotero library ID
- `display_name`
- `credential_ref`: keychain/secret reference, never raw token in project files
- `last_sync_at`

Zotero import candidate:
- `zotero_item_key`
- `library_id`
- `title`
- `authors`
- `year/date`
- `DOI`
- `URL`
- `abstract`
- `citation_key`
- `attachment_refs`
- `selected_for_import`

Mapping to literature metadata:
- Zotero title -> literature title
- creators/authors -> authors
- publication title -> venue/publication
- date -> year/date
- DOI/URL copied directly
- abstract note copied if available
- Zotero item key stored as provenance

### Engine/API Surface

Add Zotero actions:
- `get_zotero_auth_status(project_id) -> ZoteroAuthStatus`
- `start_zotero_auth(project_id) -> ZoteroAuthRequest`
- `complete_zotero_auth(project_id, callback_payload) -> ZoteroAuthStatus`
- `list_zotero_libraries(project_id) -> list[ZoteroLibrary]`
- `search_zotero_items(project_id, query?) -> list[ZoteroImportCandidate]`
- `import_zotero_items(project_id, candidate_ids, import_options) -> ImportResult`

Authentication default:
- Prefer browser-based OAuth/API-key workflow supported by Zotero.
- Store credentials in keychain/secret storage.
- Never write raw Zotero credentials to repo, project files, transcripts, or logs.

### UI And Commands

Import modal:
- Add Zotero as a source under literature import.
- If unauthenticated, show login/connect flow.
- If authenticated, show searchable Zotero item list.
- User selects one or more items and attached files.
- Imported metadata opens the same literature metadata approval flow as normal literature import.

Attachment handling:
- Markdown attachments go through Markdown literature import.
- PDF/image/document attachments go through OCR import first, then literature metadata approval.
- Metadata-only Zotero items can still create literature records with no local full text.

Shortcut row and palette:
- Required commands:
  - `Import from Zotero`
  - `Reconnect Zotero`
  - `Search Zotero Library`

### Implementation Batches

1. Auth and secret contract
   - Add auth status, connect flow, and credential-ref storage.
2. Zotero metadata search
   - List/search libraries and items.
3. Metadata mapping
   - Convert Zotero candidates into literature metadata approval candidates.
4. Attachment import
   - Route attachments through Markdown/OCR literature import pipeline.
5. UI integration
   - Add Zotero source in import modal and command palette.

### Test Plan

- Unauthenticated Zotero import prompts for connect flow.
- Auth completion stores only credential references in project state.
- Zotero search returns import candidates with metadata fields.
- Metadata-only import creates literature record.
- PDF attachment import routes through OCR/literature pipeline.
- Markdown attachment import skips OCR and runs literature metadata approval.
- Failed auth or expired credentials shows reconnect action.
- Command palette contains Zotero commands.

References:
- [Zotero Web API basics](https://www.zotero.org/support/dev/web_api/v3/basics)
- [Zotero OAuth](https://www.zotero.org/support/dev/web_api/v3/oauth)

## Milestone 14: Formatting Bar

Lane: `feat-formatting-bar` (disabled)

Intent:
- Make Markdown editing feel familiar without turning the document pane into WYSIWYG.
- Prefer semantic headings because headings support export, retrieval, and document structure better than manual visual formatting.

### Data And Behavior

Formatting operation:
- `document_id`
- `selection_range` or `cursor_offset`
- `format`: bold, italic, underline, heading_1, heading_2, heading_3, heading_4, heading_5, heading_6
- `before_hash`
- `after_hash`
- undoable edit operation reference

Markdown rules:
- Bold wraps selected text in `**text**`.
- Italic wraps selected text in `*text*`.
- Underline uses HTML `<u>text</u>` only where explicitly supported; otherwise command is disabled or marked non-portable.
- Heading commands operate on the current line or selected lines and write Markdown heading prefixes.
- Heading commands should replace existing heading prefix rather than stacking `#` characters.
- Empty selection formatting inserts paired markers with cursor between them where practical.

### Engine/API Surface

Add formatting actions:
- `apply_inline_format(document_id, selection_range, format) -> EditResult`
- `apply_heading_level(document_id, line_range, level) -> EditResult`
- `list_formatting_commands(document_id, selection_state) -> list[FormattingCommandState]`

Integration requirement:
- Formatting operations must record undoable edit operations once Milestone 10 is enabled.
- Formatting must preserve Markdown source text as the backing document truth.

### UI And Commands

Formatting bar:
- Appears above or within the document pane when lane is enabled.
- Controls: Bold, Italic, Underline, H1, H2, H3, H4, H5, H6.
- Headers are visually grouped before inline styles or clearly marked as structure.
- The UI should not imply WYSIWYG storage; it is a Markdown helper.

Shortcut row and palette:
- Add a dedicated formatting row when lane is enabled.
- Required commands:
  - `Bold`
  - `Italic`
  - `Underline`
  - `Heading 1`
  - `Heading 2`
  - `Heading 3`
  - `Heading 4`
  - `Heading 5`
  - `Heading 6`

### Implementation Batches

1. Formatting edit helpers
   - Add pure text transforms and tests for inline and heading formatting.
2. Document pane actions
   - Wire transforms to selection/cursor state and undo stack.
3. Formatting bar UI
   - Add buttons and disabled/enabled states.
4. Shortcut and palette integration
   - Add top row and command-palette commands.
5. Export/retrieval compatibility checks
   - Verify heading transforms produce clean Markdown structure.

### Test Plan

- Bold wraps selected text exactly once.
- Italic wraps selected text exactly once.
- Underline writes supported syntax or is disabled with clear status.
- Heading 2 turns current line into `## Heading`.
- Heading command replaces existing heading level rather than stacking markers.
- Formatting with no selection inserts marker pair or applies to current line as appropriate.
- Formatting operations are undoable when editor basics are enabled.
- Formatting bar commands appear in command palette.
- Backing document remains Markdown text, not WYSIWYG state.
