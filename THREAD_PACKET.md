## Thread Handoff Packet

- Branch/lane: `codex/feat-retrieval-fts` / `feat-retrieval-fts`; merge target: current branch tip after this fixer commit. This packet is regenerated against the actual branch-tip correction range from `adfa8cdadd43747ffbcb612e4151e262b13e52ca..8608095c1` plus this metadata-only fixer commit; the final HEAD SHA is reported in the fixer response because a commit cannot contain its own SHA.
- Review basis: branch-tip retrieval correction range through pre-fixer tip `8608095c1`, explicitly including `74e550e2b8cc7afef28758cd29de270b7e023322`. The earlier FTS-first implementation remains in scope as the baseline; this packet no longer claims that `74e550e2b` is metadata-only.
- Scope completed: the branch-tip correction range keeps retrieval FTS-first while hardening engine retrieval constraints, cache snapshots, basket-promotion context, and generic context-bundle fallback. The boolean normalization in `src/qual/engine/retrieval/__init__.py` advances the canonical demo-path step `retrieve relevant material` by ensuring engine query constraints such as `require_citations` and `prefer_exact_matches` are normalized deterministically instead of treating every non-empty string as true. `build_retrieval_context_bundle_from_result` now falls through from invalid direct context snapshots to canonical source/payload reconstruction.
- Canonical demo-path step advanced: `retrieve relevant material` is the primary step advanced by boolean constraint normalization and FTS cache/query canonicalization. `promote or gather context into the basket` is advanced only by preserving deterministic basket-promotion references already produced by the FTS-first retrieval result.
- Tasks completed: 1. `retrieve relevant material`: normalize engine facade boolean constraints for `require_citations` and `prefer_exact_matches` without widening retrieval modes. 2. `retrieve relevant material`: keep FTS cache keys and retrieval source/query snapshots deterministic for repeated engine retrieval. 3. `promote or gather context into the basket`: preserve basket-promotion item refs, item IDs, provenance, and sparse payload reconstruction from canonical FTS result data. 4. `retrieve relevant material`: harden generic context-bundle helper fallback from invalid direct context snapshots to canonical source/payload reconstruction and verify the existing retrieval regression surface plus full local gates.
- Files changed in the branch-tip correction range: `.codex/kickoff_packets/feat-retrieval-fts.md`, `.codex/lane_meta/feat-retrieval-fts.json`, `THREAD_PACKET.md`, `src/qual/engine/retrieval/__init__.py`, `src/qual/engine/retrieval/fts_strategy.py`, `src/qual/engine/retrieval/payload.py`, `src/qual/retrieval/service.py`, `tests/unit/test_unified_retrieval.py`.
- Source/test files changed in the branch-tip correction range: `src/qual/engine/retrieval/__init__.py`, `src/qual/engine/retrieval/fts_strategy.py`, `src/qual/engine/retrieval/payload.py`, `src/qual/retrieval/service.py`, `tests/unit/test_unified_retrieval.py`.
- Budget/risk: high-risk retrieval work because it touches engine retrieval behavior and approved shared regression coverage. Task budget `4/4`; branch-tip correction range size before this metadata-only fixer is `8 files changed, 459 insertions(+), 139 deletions(-)`, net `+320`, which exceeds the high-risk `<=300` net LOC size budget and is therefore called out for re-review rather than hidden. No integrator-locked files are changed, and PageIndex/embeddings remain fallback-only.
- Roadmap/vision: `ROADMAP.md` MVP focus for `feat-retrieval-fts`, Milestone 3 Product Readiness generation provenance contract, and Milestone 4 Retrieval Layer; `PRODUCT_VISION.md` capability 2 Retrieval-first context handling and capability 6 Auditable state and workflow; `ARCHITECTURE.md` engine-side retrieval remains deterministic and structured for downstream consumers. Routing/provider impact: none; proposed `README.md` patch text: none.
- Commands run for the corrected merge candidate: `make scope-check` PASS; `./quality-format.sh --check` PASS; `./quality-lint.sh` PASS; `./quality-test.sh` PASS, 125 tests; `./typecheck-test.sh` PASS; `make ci` PASS, 125 tests.
- Required gates: `make scope-check` PASS; `./quality-format.sh --check` PASS; `./quality-lint.sh` PASS; `./quality-test.sh` PASS; `./typecheck-test.sh` PASS; `make ci` PASS.
- Risks/blockers: the editable handoff packet now explicitly includes `74e550e2b8cc7afef28758cd29de270b7e023322` and the branch-tip source files. The `.codex` packet mirrors `.codex/kickoff_packets/feat-retrieval-fts.md` and `.codex/lane_meta/feat-retrieval-fts.json` could not be updated in this sandbox because writes and xattr removal fail with `Operation not permitted` from `com.apple.provenance`; reviewers should use this `THREAD_PACKET.md` refresh plus the final fixer SHA as the source of truth. Remaining risk is medium/high review risk from high-risk retrieval scope and size-budget overage. The change intentionally does not widen retrieval strategy scope, does not make PageIndex or embeddings required paths, and keeps Textual UI lanes disabled.
