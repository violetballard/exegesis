## Thread Handoff Packet

- Branch/lane: `codex/feat-retrieval-fts` / `feat-retrieval-fts`; merge target: current branch tip after this fixer commit. This packet was refreshed from pre-fixer tip `74e550e2b8cc7afef28758cd29de270b7e023322`; the final HEAD SHA is reported in the fixer response because a commit cannot contain its own SHA.
- Review basis: current branch-tip delta from `74e550e2b8cc7afef28758cd29de270b7e023322` through this fixer commit. The earlier reviewed retrieval implementation remains FTS-first; this pass adds one owned-path engine retrieval context-bundle fallback hardening change.
- Scope completed: `build_retrieval_context_bundle_from_result` no longer treats a non-dict direct `retrieval_context_bundle()` return as authoritative. Generic retrieval result adapters now fall through to the canonical source-bundle or downstream-payload reconstruction paths, preserving deterministic FTS-first context output for basket promotion and later revise/apply consumers.
- Canonical demo-path step advanced: this supports the `retrieve relevant material` and `promote or gather context into the basket` steps by keeping context-bundle reconstruction stable when a compatibility source exposes sparse or invalid direct context data but still exposes canonical retrieval source/payload data.
- Tasks completed: 1. `retrieve relevant material`: harden generic context-bundle helper fallback from invalid direct context snapshots to canonical source/payload reconstruction. 2. `promote or gather context into the basket`: preserve basket promotion fields through the reconstructed context bundle. 3. `retrieve relevant material`: keep the FTS-first policy unchanged and leave PageIndex/embeddings deferred. 4. `retrieve relevant material`: verify focused fallback behavior, the existing retrieval regression surface, and full local gates.
- Files changed in this fixer pass: `THREAD_PACKET.md`, `src/qual/engine/retrieval/payload.py`.
- Source/test files changed in this fixer pass: `src/qual/engine/retrieval/payload.py`.
- Budget/risk: this pass stays within retrieval-owned source paths plus this handoff packet. No integrator-locked files are changed; no shared regression file is changed in this fixer pass. Task budget `4/4`; source size `1 deletion(-)`, net `-1`, within the high-risk `<=300` net LOC limit.
- Roadmap/vision: `ROADMAP.md` Milestone 4 Retrieval Layer and Milestone 3 Product Readiness generation provenance contract; `PRODUCT_VISION.md` capability 2 Retrieval-first context handling and capability 3 Auditable generation; routing/provider impact: none; proposed `README.md` patch text: none.
- Commands run for this fixer pass: `python - <<'PY' ... build_retrieval_context_bundle_from_result(...) ... PY` FAIL on the first synthetic assertion because the fixture asserted top-level provenance fields not present in the sparse source bundle; rerun adjusted to the canonical source/downstream fields and PASS; `python -m unittest tests.unit.test_unified_retrieval -v` PASS, 56 tests; `make scope-check` PASS; `./quality-format.sh --check` PASS; `./quality-lint.sh` PASS; `./quality-test.sh` PASS, 125 tests; `./typecheck-test.sh` PASS; `make ci` PASS, 125 tests.
- Required gates: `make scope-check` PASS; `./quality-format.sh --check` PASS; `./quality-lint.sh` PASS; `./quality-test.sh` PASS; `./typecheck-test.sh` PASS; `make ci` PASS.
- Risks/blockers: no implementation blocker is known. The change intentionally does not widen retrieval strategy scope, does not touch PageIndex or embeddings behavior, and keeps Textual UI lanes disabled.
