## Thread Handoff Packet
- Branch/lane: `codex/feat-retrieval-fts` / `feat-retrieval-fts`; review target: final branch tip from this fixer pass, SHA reported in response; reviewed range: `378cf9a74a3658058079a32f186fcd254c4a4034..final fixer commit`.
- Scope completed: full branch-tip runtime/test range is retained: FTS-only `fetch_excerpt`, deterministic `retrieval_basket_promotion` context/source bundles, source-bundle fingerprints, and engine payload rehydration for sparse downstream inputs; PageIndex-only excerpt IDs fail closed and PageIndex/embeddings remain compatibility-only deferred paths.
- Tasks completed: 1. FTS-first excerpt lookup and fail-closed PageIndex rejection. 2. Deterministic basket-promotion/source-bundle snapshots with stable IDs, fingerprints, citation status, query scope, intent, and date range. 3. Engine payload rehydration for compact source/context bundles. 4. Shared regression coverage for excerpt backfill, facade exports, payload normalization, provenance helpers, and basket-promotion snapshots.
- Files changed: `.codex/kickoff_packets/feat-retrieval-fts.md`, `.codex/lane_meta/feat-retrieval-fts.json`, `THREAD_PACKET.md`, `src/qual/engine/retrieval/payload.py`, `src/qual/retrieval/service.py`, `tests/unit/test_unified_retrieval.py`.
- Budget: high-risk because `tests/unit/test_unified_retrieval.py` is approved shared coverage; task budget `4/4`, file limit `6/8`; final range must be read from git shortstat, with this packet intentionally compact to keep the branch-tip range within `<=300` net LOC.
- Roadmap/vision: `ROADMAP.md` Milestone 3 Product Readiness and Milestone 4 Retrieval Layer; `PRODUCT_VISION.md` capability 2 retrieval-first context handling and capability 6 auditable state/workflow; routing/provider impact: none.
- Canonical demo path: basket-promotion changes advance `vault -> context -> run -> patch -> export` at `context -> run` because the run step needs deterministic references to promote retrieved chunks into the context basket and preserve generation provenance; this is not second-order work.
- Commands run: `make scope-check` PASS; `./quality-format.sh --check` PASS; `./quality-lint.sh` PASS; `./quality-test.sh` PASS; `./typecheck-test.sh` PASS; `make ci` PASS.
- Risks/blockers: `.codex/kickoff_packets/feat-retrieval-fts.md` and `.codex/lane_meta/feat-retrieval-fts.json` still contain stale branch-head framing and could not be edited in this sandboxed fixer pass because writes were rejected as outside the writable project; use this packet plus final response SHA as the corrected re-review surface. Residual risk: broader promotion orchestration remains separate high-risk work.
