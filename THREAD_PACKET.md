## Thread Handoff Packet

- Branch/lane: `codex/feat-retrieval-fts` / `feat-retrieval-fts`; merge target: current branch tip after this fixer commit. This packet was refreshed from pre-fixer tip `1e78c7f5b99dd50c017d79e9276338c93579aa07`; the final HEAD SHA is reported in the fixer response because a commit cannot contain its own SHA.
- Review basis: current branch-tip delta from `1e78c7f5b99dd50c017d79e9276338c93579aa07` through this fixer commit. The earlier reviewed retrieval implementation remains FTS-first; this pass adds one owned-path engine retrieval facade hardening change.
- Scope completed: the canonical engine retrieval query constructor now normalizes JSON/CLI-style boolean constraint values deterministically. Serialized values such as `"false"` for `require_citations` and `"true"` for `prefer_exact_matches` no longer drift into Python truthiness before the FTS-first retrieval path receives the query.
- Canonical demo-path step advanced: this supports the `retrieve relevant material` step by keeping engine-facing retrieval constraints stable and predictable before FTS retrieval constructs doc/excerpt results for basket promotion and later revise/apply steps.
- Tasks completed: 1. `retrieve relevant material`: normalize serialized boolean retrieval constraints in the canonical engine query builder. 2. `retrieve relevant material`: preserve the existing FTS-first path and deferred PageIndex/embeddings policy without adding alternate retrieval behavior. 3. `retrieve relevant material`: verify the existing retrieval regression surface and full local gates against the updated query constructor.
- Files changed in this fixer pass: `THREAD_PACKET.md`, `src/qual/engine/retrieval/__init__.py`.
- Source/test files changed in this fixer pass: `src/qual/engine/retrieval/__init__.py`.
- Budget/risk: this pass stays within retrieval-owned source paths plus this handoff packet. No integrator-locked files are changed; no shared regression file is changed in this fixer pass. Task budget `3/4`; source size `25 insertions(+), 2 deletions(-)`, net `+23`, within the high-risk `<=300` net LOC limit.
- Roadmap/vision: `ROADMAP.md` Milestone 4 Retrieval Layer; `PRODUCT_VISION.md` capability 2 Retrieval-first context handling and capability 3 Auditable generation; routing/provider impact: none; proposed `README.md` patch text: none.
- Commands run for this fixer pass: `python - <<'PY' ... build_retrieval_query(...) ... PY` PASS, serialized boolean constraints normalize correctly; `python -m unittest tests.unit.test_unified_retrieval -v` PASS, 56 tests; `make scope-check` PASS; `./quality-format.sh --check` PASS; `./quality-lint.sh` PASS; `./quality-test.sh` PASS, 125 tests; `./typecheck-test.sh` PASS; `make ci` PASS, 125 tests.
- Risks/blockers: no implementation blocker is known. The change intentionally does not widen retrieval strategy scope, does not touch PageIndex or embeddings behavior, and keeps Textual UI lanes disabled.
