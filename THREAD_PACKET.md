## Thread Handoff Packet

- Branch/lane: `codex/feat-retrieval-fts` / `feat-retrieval-fts`; merge target: current branch tip after this fixer commit; final HEAD SHA is reported in the fixer response because a commit cannot contain its own SHA.
- Review basis: narrow implementation range only, `378cf9a74a3658058079a32f186fcd254c4a4034..adfa8cdadd43747ffbcb612e4151e262b13e52ca`. The cumulative branch range `d7fd5d200358287fa42a18d39e2b277463b9b69f..adfa8cdadd43747ffbcb612e4151e262b13e52ca` is not the review basis for this handoff.
- Required fixes addressed: regenerated the packet around one coherent narrow review basis; listed only the files changed in that reviewed implementation range; added the direct canonical demo-path statement; clarified that the only shared scope is approved shared regression coverage in `tests/unit/test_unified_retrieval.py`; no integrator-locked files are in the reviewed implementation range.
- Scope completed: FTS-only excerpt lookup now uses the canonical FTS-first retrieval path, and PageIndex-only excerpt IDs fail closed under shared regression coverage. This packet does not request review of cumulative retrieval-facade, payload, planner, or packet metadata work outside the narrow range.
- Canonical demo-path step advanced: this makes the `retrieve relevant material` step more real by ensuring excerpt lookup is FTS-only, deterministic, and provenance-backed for basket promotion.
- Tasks completed: 1. `retrieve relevant material`: remove the PageIndex fallback from excerpt lookup so only canonical FTS-backed excerpt IDs are accepted. 2. `retrieve relevant material`: add shared regression coverage proving PageIndex-only excerpt IDs fail closed while FTS-backed excerpt lookup remains deterministic.
- Files changed in `378cf9a74a3658058079a32f186fcd254c4a4034..adfa8cdadd43747ffbcb612e4151e262b13e52ca`: `src/qual/retrieval/service.py`, `tests/unit/test_unified_retrieval.py`.
- Budget/risk: high-risk/shared only because `tests/unit/test_unified_retrieval.py` is approved shared-by-approval regression coverage; no integrator-locked edits are present. Task budget `2/4`; file count `2/8`; size `28 insertions(+), 31 deletions(-)`, net `-3`, within the `<=300` net LOC limit. Because the cumulative branch range is not the review basis, no high-risk size exception is requested for this handoff.
- Roadmap/vision: `ROADMAP.md` Milestone 4 Retrieval Layer; `PRODUCT_VISION.md` capability 2 Retrieval-first context handling and capability 3 Auditable generation; routing/provider impact: none; proposed `README.md` patch text: none.
- Commands run for this corrected merge candidate: `make scope-check` PASS; `./quality-format.sh --check` PASS; `./quality-lint.sh` PASS; `./quality-test.sh` PASS, 125 tests; `./typecheck-test.sh` PASS; `make ci` PASS, 125 tests.
- Earlier retained focused checks: `python3 -m unittest tests.unit.test_unified_retrieval -v` PASS, 56 tests; `python -m unittest tests.unit.test_unified_retrieval` PASS, 56 tests; `python -m py_compile src/qual/engine/retrieval/fts_strategy.py && python -m unittest tests.unit.test_unified_retrieval` PASS, 56 tests.
- Risks/blockers: no implementation blocker is known. Later branch-tip commits are packet-refresh or follow-on work and are outside the narrow reviewed implementation range unless a future packet explicitly changes the review basis.
