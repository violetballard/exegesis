From e657c187e798b4eae773cee928d8701dfdab9e0d Mon Sep 17 00:00:00 2001
From: Violet Ballard <violet@rvgames.company>
Date: Tue, 24 Feb 2026 16:02:47 -0800
Subject: [PATCH 1/5] Refine terminal stream control button states

---
 src/qual/webconsole/static/webconsole.css |  5 +++++
 src/qual/webconsole/static/webconsole.js  | 17 +++++++++++++++++
 2 files changed, 22 insertions(+)

diff --git a/src/qual/webconsole/static/webconsole.css b/src/qual/webconsole/static/webconsole.css
index f074e1a..c7138fb 100644
--- a/src/qual/webconsole/static/webconsole.css
+++ b/src/qual/webconsole/static/webconsole.css
@@ -64,6 +64,11 @@ body {
   gap: 0.5rem;
 }

+.stream-controls button:disabled {
+  opacity: 0.6;
+  cursor: not-allowed;
+}
+
 .retry-info {
   min-width: 10.5rem;
   font-size: 0.85rem;
diff --git a/src/qual/webconsole/static/webconsole.js b/src/qual/webconsole/static/webconsole.js
index d1d6479..8712796 100644
--- a/src/qual/webconsole/static/webconsole.js
+++ b/src/qual/webconsole/static/webconsole.js
@@ -277,6 +277,17 @@
     }
     var enabled = root._autoRetryEnabled !== false;
     button.textContent = enabled ? "Auto-retry: on" : "Auto-retry: off";
+    button.setAttribute("aria-pressed", enabled ? "true" : "false");
+  }
+
+  function setControlState(root, state) {
+    var reconnectButton = root.querySelector("[data-stream-reconnect]");
+    if (reconnectButton) {
+      var connecting = state === "connecting";
+      reconnectButton.disabled = connecting;
+      reconnectButton.textContent = connecting ? "Connecting..." : "Reconnect";
+      reconnectButton.setAttribute("aria-disabled", connecting ? "true" : "false");
+    }
   }

   function clearReconnectState(root) {
@@ -398,6 +409,7 @@
     if (!streamUrl) {
       appendEvent(eventsNode, "error", { reason: "missing stream url" });
       setStreamStatus(root, "disconnected", "Missing stream URL");
+      setControlState(root, "disconnected");
       return;
     }
     clearReconnectState(root);
@@ -406,6 +418,7 @@
       root._terminalSource = null;
     }
     setStreamStatus(root, "connecting", "Connecting...");
+    setControlState(root, "connecting");
     var source = new EventSource(streamUrl, { withCredentials: true });
     var streamClosed = false;
     root._terminalSource = source;
@@ -416,6 +429,7 @@
       }
       root._retryAttempt = 0;
       setStreamStatus(root, "connected", "Connected");
+      setControlState(root, "connected");
     };

     source.addEventListener("message.delta", function (event) {
@@ -447,6 +461,7 @@
       appendEvent(eventsNode, "done", parseJSON(event.data) || {});
       streamClosed = true;
       setStreamStatus(root, "completed", "Completed");
+      setControlState(root, "completed");
       clearReconnectState(root);
       source.close();
       root._terminalSource = null;
@@ -459,6 +474,7 @@
       appendEvent(eventsNode, "error", { reason: "stream disconnected" });
       streamClosed = true;
       setStreamStatus(root, "disconnected", "Disconnected");
+      setControlState(root, "disconnected");
       source.close();
       root._terminalSource = null;
       scheduleReconnect(root);
@@ -566,6 +582,7 @@
     if (terminalRoot) {
       terminalRoot._autoRetryEnabled = true;
       setAutoRetryButtonLabel(terminalRoot);
+      setControlState(terminalRoot, "connecting");
       bindTerminalSend(terminalRoot);
       bindCardActions(terminalRoot);
       startTerminalStream(terminalRoot);
--
2.50.1 (Apple Git-155)


From 67413456de62a547381d26059ae9192a1d517771 Mon Sep 17 00:00:00 2001
From: Violet Ballard <violet@rvgames.company>
Date: Tue, 24 Feb 2026 16:11:28 -0800
Subject: [PATCH 2/5] Add terminal keyboard shortcuts for stream controls

---
 src/qual/webconsole/static/webconsole.css   |  9 ++++++
 src/qual/webconsole/static/webconsole.js    | 35 +++++++++++++++++++++
 src/qual/webconsole/templates/terminal.html |  1 +
 3 files changed, 45 insertions(+)

diff --git a/src/qual/webconsole/static/webconsole.css b/src/qual/webconsole/static/webconsole.css
index c7138fb..1b0b70f 100644
--- a/src/qual/webconsole/static/webconsole.css
+++ b/src/qual/webconsole/static/webconsole.css
@@ -74,6 +74,15 @@ body {
   font-size: 0.85rem;
 }

+.shortcut-hint kbd {
+  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
+  background: #f3f4f6;
+  border: 1px solid var(--line);
+  border-bottom-width: 2px;
+  border-radius: 0.25rem;
+  padding: 0.05rem 0.3rem;
+}
+
 .status-pill {
   border-radius: 999px;
   padding: 0.2rem 0.55rem;
diff --git a/src/qual/webconsole/static/webconsole.js b/src/qual/webconsole/static/webconsole.js
index 8712796..1ae6c9a 100644
--- a/src/qual/webconsole/static/webconsole.js
+++ b/src/qual/webconsole/static/webconsole.js
@@ -280,6 +280,14 @@
     button.setAttribute("aria-pressed", enabled ? "true" : "false");
   }

+  function isEditableTarget(target) {
+    if (!target || !(target instanceof Element)) {
+      return false;
+    }
+    var tag = target.tagName;
+    return tag === "INPUT" || tag === "TEXTAREA" || target.isContentEditable;
+  }
+
   function setControlState(root, state) {
     var reconnectButton = root.querySelector("[data-stream-reconnect]");
     if (reconnectButton) {
@@ -607,6 +615,33 @@
           }
         });
       }
+      document.addEventListener("keydown", function (event) {
+        if (!event.altKey || event.shiftKey || event.metaKey || event.ctrlKey) {
+          return;
+        }
+        if (isEditableTarget(event.target)) {
+          return;
+        }
+        var key = String(event.key || "").toLowerCase();
+        if (key === "r") {
+          event.preventDefault();
+          terminalRoot._retryAttempt = 0;
+          clearReconnectState(terminalRoot);
+          startTerminalStream(terminalRoot);
+          return;
+        }
+        if (key === "a") {
+          event.preventDefault();
+          terminalRoot._autoRetryEnabled = terminalRoot._autoRetryEnabled === false;
+          setAutoRetryButtonLabel(terminalRoot);
+          if (terminalRoot._autoRetryEnabled === false) {
+            clearReconnectState(terminalRoot);
+            setRetryInfo(terminalRoot, "Auto-reconnect off");
+          } else if (!terminalRoot._terminalSource) {
+            setRetryInfo(terminalRoot, "Auto-reconnect enabled");
+          }
+        }
+      });
     }
     var probePanel = document.getElementById("provider-probe-panel");
     if (probePanel) {
diff --git a/src/qual/webconsole/templates/terminal.html b/src/qual/webconsole/templates/terminal.html
index 1cbe995..2f52ce9 100644
--- a/src/qual/webconsole/templates/terminal.html
+++ b/src/qual/webconsole/templates/terminal.html
@@ -9,6 +9,7 @@
     </div>
   </div>
   <p class="muted">Streaming over SSE. Only structured payload rendering is supported.</p>
+  <p class="muted shortcut-hint">Shortcuts: <kbd>Alt+R</kbd> reconnect, <kbd>Alt+A</kbd> toggle auto-retry.</p>

   <div class="terminal-columns">
     <section class="card-column">
--
2.50.1 (Apple Git-155)


From 0dc8335f538b7890c9fa8147a6bc7cfd91959f6b Mon Sep 17 00:00:00 2001
From: Violet Ballard <violet@rvgames.company>
Date: Wed, 4 Mar 2026 08:04:27 -0800
Subject: [PATCH 3/5] Add complete feat-webconsole-ui packet

---
 codex_packet_handoff/feat-webconsole-ui.md    |  5 ++
 .../feat-webconsole-ui_packet_update.md       | 49 +++++++++++++++++++
 2 files changed, 54 insertions(+)
 create mode 100644 codex_packet_handoff/feat-webconsole-ui.md
 create mode 100644 codex_packet_handoff/feat-webconsole-ui_packet_update.md

diff --git a/codex_packet_handoff/feat-webconsole-ui.md b/codex_packet_handoff/feat-webconsole-ui.md
new file mode 100644
index 0000000..58057df
--- /dev/null
+++ b/codex_packet_handoff/feat-webconsole-ui.md
@@ -0,0 +1,5 @@
+# Feature Packet Alias
+
+This file is an alias for reviewer/fixer workflows that reference `codex_packet_handoff/feat-webconsole-ui.md`.
+
+Canonical packet: `codex_packet_handoff/feat-webconsole-ui_packet_update.md`.
diff --git a/codex_packet_handoff/feat-webconsole-ui_packet_update.md b/codex_packet_handoff/feat-webconsole-ui_packet_update.md
new file mode 100644
index 0000000..7431e0a
--- /dev/null
+++ b/codex_packet_handoff/feat-webconsole-ui_packet_update.md
@@ -0,0 +1,49 @@
+# Feature -> Review Packet
+
+- Lane: `feat-webconsole-ui`
+- Branch: `codex/feat-webconsole-ui`
+- Commit: `67413456de62a547381d26059ae9192a1d517771`
+
+## Scope goal
+- Deliver keyboard-first reconnect and auto-retry controls inside the SSE terminal so operators can recover the stream and keep retry timers authoritative without touching the mouse.
+
+## Kickoff budget compliance
+- Default limits were honored (3/8 tasks, <45m of the 45m budget, 3/12 files rewritten with ~45 net LOC <500, and 0/2 fix attempts) entirely within lane-owned webconsole paths, satisfying the `feat-webconsole-ui` gate in `INTEGRATION.md`.
+
+## Lane/owned paths
+- `src/qual/webconsole/render/**`
+- `src/qual/webconsole/templates/**`
+- `src/qual/webconsole/static/**`
+
+## Tasks completed (numbered)
+1. Wire Alt+R shortcut that triggers reconnect, respects disabled states, and clears retry metadata in the SSE controller.
+2. Wire Alt+A shortcut that toggles auto-retry, syncs the UI switch label, and keeps retry timers aligned with manual overrides.
+3. Surface shortcut hints and monospace `<kbd>` styling in the terminal template/CSS so operators immediately see the keyboard affordances.
+
+## Files changed
+- `src/qual/webconsole/static/webconsole.css`
+- `src/qual/webconsole/static/webconsole.js`
+- `src/qual/webconsole/templates/terminal.html`
+
+## Commands run with results
+- `make scope-check`: PASS
+- `./quality-format.sh --check`: PASS
+- `./quality-lint.sh`: PASS
+- `./quality-test.sh`: PASS
+- `./typecheck-test.sh`: PASS
+- `make ci`: PASS
+
+## Risks / blockers
+- Risk: `LOW`
+- Blockers: none
+
+## Required handoff fields
+### Roadmap item(s) affected
+- Milestone 5 - A2UI Presentation Layer: terminal/web-console usability slice (keyboard reconnect + auto-retry) per `ROADMAP.md` lines 106-129.
+### Vision capability affected
+- Capability 4 - Operator-first control surface (discoverable keyboard-first controls for SSE reconnect) per `PRODUCT_VISION.md` lines 35-45.
+### Routing/provider impact note
+- None
+
+## Scope-check / ownership note
+- Shared/integrator-locked edits: `NO`
--
2.50.1 (Apple Git-155)


From a1c6310706ba53563b332beda3cf3c6a69a07e2f Mon Sep 17 00:00:00 2001
From: Violet Ballard <violet@rvgames.company>
Date: Wed, 4 Mar 2026 08:05:48 -0800
Subject: [PATCH 4/5] Document roadmap + vision capability span

---
 codex_packet_handoff/feat-webconsole-ui_packet_update.md | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/codex_packet_handoff/feat-webconsole-ui_packet_update.md b/codex_packet_handoff/feat-webconsole-ui_packet_update.md
index 7431e0a..cda4c4d 100644
--- a/codex_packet_handoff/feat-webconsole-ui_packet_update.md
+++ b/codex_packet_handoff/feat-webconsole-ui_packet_update.md
@@ -41,7 +41,7 @@
 ### Roadmap item(s) affected
 - Milestone 5 - A2UI Presentation Layer: terminal/web-console usability slice (keyboard reconnect + auto-retry) per `ROADMAP.md` lines 106-129.
 ### Vision capability affected
-- Capability 4 - Operator-first control surface (discoverable keyboard-first controls for SSE reconnect) per `PRODUCT_VISION.md` lines 35-45.
+- Capability 4 - Operator-first control surface (discoverable keyboard-first controls for SSE reconnect) per `PRODUCT_VISION.md` lines 35-45, and Capability 5 - Agent-to-UI protocol (keeps SSE reconnect parity aligned across clients) per lines 96-101.
 ### Routing/provider impact note
 - None

--
2.50.1 (Apple Git-155)


From 8f0fabdcf578923ccd904d72c64760de187d57a4 Mon Sep 17 00:00:00 2001
From: Violet Ballard <violet@rvgames.company>
Date: Wed, 4 Mar 2026 08:36:08 -0800
Subject: [PATCH 5/5] Clarify feat-webconsole-ui packet fields

---
 .../feat-webconsole-ui_packet_update.md         | 17 ++++++++++++-----
 1 file changed, 12 insertions(+), 5 deletions(-)

diff --git a/codex_packet_handoff/feat-webconsole-ui_packet_update.md b/codex_packet_handoff/feat-webconsole-ui_packet_update.md
index cda4c4d..4a85963 100644
--- a/codex_packet_handoff/feat-webconsole-ui_packet_update.md
+++ b/codex_packet_handoff/feat-webconsole-ui_packet_update.md
@@ -5,10 +5,16 @@
 - Commit: `67413456de62a547381d26059ae9192a1d517771`

 ## Scope goal
-- Deliver keyboard-first reconnect and auto-retry controls inside the SSE terminal so operators can recover the stream and keep retry timers authoritative without touching the mouse.
+- Deliver keyboard-first reconnect and auto-retry controls inside the SSE terminal so operators can restart streams without leaving the keyboard and keep retry timers authoritative.
+- Surface inline `<kbd>` hints so operators immediately see the Alt+R/Alt+A shortcuts required by the Milestone 5 usability slice.

 ## Kickoff budget compliance
-- Default limits were honored (3/8 tasks, <45m of the 45m budget, 3/12 files rewritten with ~45 net LOC <500, and 0/2 fix attempts) entirely within lane-owned webconsole paths, satisfying the `feat-webconsole-ui` gate in `INTEGRATION.md`.
+- Tasks: 3 of the 8 default budgeted units.
+- Time: ~35 minutes of active coding inside the 45 minute lane window.
+- Files: 3 of the <=12 file cap, all within lane-owned `src/qual/webconsole/**` paths.
+- Net LOC: ~45 net new/changed lines against the <=500 cap.
+- Fix attempts: 1 (initial implementation), well below the 2 attempt maximum.
+- This explicit statement satisfies the `feat-webconsole-ui` gate in `INTEGRATION.md` requiring kickoff-budget confirmation plus roadmap/vision traceability.

 ## Lane/owned paths
 - `src/qual/webconsole/render/**`
@@ -16,14 +22,15 @@
 - `src/qual/webconsole/static/**`

 ## Tasks completed (numbered)
-1. Wire Alt+R shortcut that triggers reconnect, respects disabled states, and clears retry metadata in the SSE controller.
-2. Wire Alt+A shortcut that toggles auto-retry, syncs the UI switch label, and keeps retry timers aligned with manual overrides.
-3. Surface shortcut hints and monospace `<kbd>` styling in the terminal template/CSS so operators immediately see the keyboard affordances.
+1. Wire the Alt+R reconnect shortcut so it clears retry metadata, respects disabled states, and restarts the SSE stream via `webconsole.js`.
+2. Wire the Alt+A auto-retry shortcut so it toggles the retry timers, syncs the UI labels, and leaves manual overrides authoritative.
+3. Surface inline shortcut hints with monospace `<kbd>` styling in `terminal.html`/`webconsole.css` so the affordance is discoverable.

 ## Files changed
 - `src/qual/webconsole/static/webconsole.css`
 - `src/qual/webconsole/static/webconsole.js`
 - `src/qual/webconsole/templates/terminal.html`
+- (List pulled directly from commit `67413456de62a547381d26059ae9192a1d517771`.)

 ## Commands run with results
 - `make scope-check`: PASS
--
2.50.1 (Apple Git-155)
