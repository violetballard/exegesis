# Thread Packet Pointer

Canonical handoff contract lives in `THREAD_PACKET.md`.

## Current Review Focus

- Lane: `feat-commands`
- Branch: `codex/feat-commands`
- Review scope: command-catalog contract hardening plus regression coverage for alias-level parser drift
- Canonical demo-path step advanced: `preview and apply or reject a patch`
- Explicit re-review statement: the `diff-preview` patch-review CLI surface now fails fast if alias-level parser drift changes the `diff` token while canonical command names still appear stable.

## Reviewed Files

- `src/qual/commands/catalog.py`
- `tests/unit/test_commands_catalog.py`
- `THREAD.md`
- `THREAD_PACKET.md`
- `handoff_packets/feat-commands.md`

## Required Gates

- `make scope-check`
- `./quality-format.sh --check`
- `./quality-lint.sh`
- `./quality-test.sh`
- `./typecheck-test.sh`
- `make ci`
