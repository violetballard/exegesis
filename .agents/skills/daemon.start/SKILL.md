---
name: daemon.start
description: Start the event-driven coordinator daemon if not already running.
---

Run from repo root:
- `python codex_packet_handoff/tools/daemon_ctl.py start`

Then print:
- whether it started or was already running
- daemon PID
- daemon log path
