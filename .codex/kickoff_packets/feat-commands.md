# Lane Kickoff: feat-commands

- Branch: `codex/feat-commands`
- Lane/owned paths: `src/qual/commands/**`
- Scope goal: Retitle the feat-commands handoff to match the actual docs-only branch history and remove unsupported code/test claims.

### Priority outcomes
1. Keep the handoff inventory honest about what the branch actually changed.
2. Remove unsupported code/test claims from the review packet and lane metadata.
3. Preserve a truthful lane record for the next implementation pass.

### Guardrails
- Stay in lane-owned command paths for future implementation work.
- Do not claim shared-file approval unless the branch diff actually includes those files.
- Keep review packets synchronized with the real branch delta.
- Keep work aligned to the current MVP: engine, FTS retrieval, A2UI, patch/export flow.
- Do not introduce new web-facing surfaces.
