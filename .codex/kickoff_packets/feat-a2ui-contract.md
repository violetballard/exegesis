# Lane Kickoff: feat-a2ui-contract

- Branch: `codex/feat-a2ui-contract`
- Lane/owned paths: `src/qual/ui/a2ui.py`
- Scope goal: Record the review-packet alignment for `feat-a2ui-contract` so the lane handoff is auditable and accurately describes the fallback allowlist hardening in `src/qual/ui/a2ui.py` without claiming tests or terminal rendering changes.

### Priority outcomes
1. Keep the reviewed file list limited to `src/qual/ui/a2ui.py`.
2. Keep the scope statement aligned with the fallback allowlist hardening change.
3. Keep the packet language separated from unsupported test or terminal-rendering claims.

### Guardrails
- No unsupported test or terminal-rendering claims in this kickoff packet.
- Keep the file list auditable against the actual commit diff.
- Favor a small, stable single-file handoff over feature-description drift.
