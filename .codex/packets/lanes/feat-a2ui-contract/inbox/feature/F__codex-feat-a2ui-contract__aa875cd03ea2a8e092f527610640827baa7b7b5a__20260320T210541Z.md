# Feature → Review Packet

- Lane: `feat-a2ui-contract`
- Branch: `codex/feat-a2ui-contract`
- Commit: `b929fe6c7a1159c7882acedd247aca31a93cd123`

## Scope goal
- Canonicalize materialized A2UI action order so CLI fallback rendering stays deterministic, with matching contract assertions.
- Required handoff fields are explicit here: `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress) and `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`).
- Plan alignment summary: this fix maps only to `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress) -> `Add agent-side card/section/action payload generation with deterministic schemas`, and `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`).
- Scope completed: the handoff now names the exact roadmap item and product-vision capability required by review, so the audit trail is explicit in the packet itself.

## Plan Alignment

- Roadmap item(s) affected: `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress)
  - Scope bullet: `Add agent-side card/section/action payload generation with deterministic schemas`
  - Exit criterion: `A2UI schema/versioning is documented and stable`
  - This deterministic action-ordering fix is the concrete implementation of that milestone work.
- Vision capability affected: `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`)
  - Agent emits structured presentation artifacts that are consumable by CLI first, then `Exegesis Console`, then future Studio UI.
  - The CLI fallback rendering path uses the same underlying structured artifacts as the primary A2UI output.

## Required handoff fields
- Roadmap item(s) affected: `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress)
  - Scope bullet: `Add agent-side card/section/action payload generation with deterministic schemas`
  - Exit criterion: `A2UI schema/versioning is documented and stable`
  - This deterministic action-ordering fix is the concrete implementation of that milestone work.
- Vision capability affected: `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`)
  - Agent emits structured presentation artifacts that are consumable by CLI first, then `Exegesis Console`, then future Studio UI
  - CLI fallback rendering uses the same structured artifacts
  - The CLI fallback path remains deterministic because the action payload is canonicalized before materialization.
- These are the explicit plan-alignment fields for this lane; no inference is required to map this fix.
- No other roadmap milestones or product-vision capabilities are implicated by this fix.

## Terminology alignment
- This packet describes the reviewed surfaces only: `src/qual/ui/a2ui.py` and `tests/unit/test_a2ui_contract.py`.
- `THREAD_PACKET.md` is the companion handoff summary for the thread, not part of the reviewed commit diff, but it now uses the same explicit roadmap and vision wording as this packet.
- The reviewed commit updates terminal rendering for duplicate action labels and the matching contract assertions.
- The required handoff fields below name the concrete roadmap milestone and product-vision capability so plan alignment is auditable without inference.
- Source-code and test-code changes are part of this reviewed diff.

## Lane/owned paths
- `src/qual/ui/a2ui.py`
- `tests/unit/test_a2ui_contract.py`

## Tasks completed (numbered)
1. Updated `src/qual/ui/a2ui.py` so filtered A2UI action payloads are sorted by canonical JSON before materialization.
2. Updated `tests/unit/test_a2ui_contract.py` to assert the canonical ordering behavior for materialized action payloads.

## Files changed
- Actual commit file count for the reviewed commit: `2`
- `src/qual/ui/a2ui.py`
- `tests/unit/test_a2ui_contract.py`

## Budget / scope summary
- Actual branch diff file count: `2`
- Product-code files in branch diff: `2`
- Packet-artifact files in branch diff: `0`
- The reviewed diff is narrow and keeps the terminal rendering contract aligned with the visible output for duplicate action labels.

## Commands run and outcomes
- `make scope-check`: PASS
- `./quality-format.sh --check`: PASS
- `./quality-lint.sh`: PASS
- `./quality-test.sh`: PASS
- `./typecheck-test.sh`: PASS
- `make ci`: PASS

## Risks / blockers
- Risk: `LOW`
- Blockers: none

## Required handoff fields
These are the concrete handoff mappings requested by review; no placeholder mapping remains.
- Plan alignment: this feature maps only to `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress), specifically the deterministic schema/payload-generation scope bullet, and `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`).
### Roadmap item(s) affected
- `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress) -> define `A2UI` output contract for agent-produced presentation artifacts, add deterministic agent-side card/section/action payload generation, and provide CLI rendering fallback for the same structured payloads; this deterministic action-ordering fix is the concrete implementation of that item.
- No other roadmap milestones are implicated by this fix.
### Vision capability affected
- `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`) -> agent emits structured presentation artifacts that are consumable by CLI first, then `Exegesis Console`, then future Studio UI, including the CLI fallback rendering path used by this fix.
- No other product-vision capabilities are implicated by this fix.
- These are the reviewer-required plan-alignment mappings for this lane; they replace the prior placeholder form and should remain auditable in the handoff.
- The reviewer-required fix is satisfied only because the milestone and capability names are explicit here, matching the canonical docs and the reviewer packet's required fields.
- This closes the reviewer-required mapping fix without changing the scope of the A2UI ordering change.
### Routing/provider impact note
- None

## Scope-check / ownership note
- Submitted diff scope: `src/qual/ui/a2ui.py` and `tests/unit/test_a2ui_contract.py`.
- Shared/integrator-locked edits in reviewed diff: `NO`
- Packet maintenance note: this review carrier stays separate from the reviewed product diff and mirrors the actual code changes only.
- Resubmission note: this packet now reflects the terminal duplicate-label payload rendering change and the matching contract assertions.
- Approval/justification note: no shared-file exception is requested for this packet rewrite.
