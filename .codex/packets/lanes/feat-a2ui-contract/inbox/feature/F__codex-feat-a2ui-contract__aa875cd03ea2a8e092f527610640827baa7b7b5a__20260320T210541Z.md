# Feature → Review Packet

- Lane: `feat-a2ui-contract`
- Branch: `codex/feat-a2ui-contract`
- Commit: `b929fe6c7a1159c7882acedd247aca31a93cd123` (runtime change under review)
- Review scope: current branch tip on `codex/feat-a2ui-contract`; follow-up commits after `b929fe6c7a1159c7882acedd247aca31a93cd123` are handoff-metadata and packet-planner regression coverage only.
- Current branch tip: `c05dca472b10ab54a4808d75d3ba3c91ed51e4d7`
- Resubmission note: metadata-only handoff update to satisfy reviewer required fix `#1` by pinning this change to the current repo's authoritative plan documents: `ROADMAP.md` Milestone 5 scope bullets `Add agent-side card/section/action payload generation with deterministic schemas` and `Provide CLI rendering fallback for the same structured payloads`, plus `PRODUCT_VISION.md` Capability 5 `Agent-to-UI protocol (A2UI)`; the saved planner handoff state and regression coverage are synced to the same wording so future packet emissions stay reviewer-auditable; no runtime behavior changed in this follow-up.
- Authoritative plan mapping: `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress) -> `MVP Focus Through 2026-05-04` task anchor for `feat-a2ui-contract`; `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`).
- Required fix `#1` is now satisfied explicitly: the required handoff fields below name the specific `ROADMAP.md` milestone/task anchor (Milestone 5: A2UI Presentation Layer (In Progress) plus the `MVP Focus Through 2026-05-04` anchor for `feat-a2ui-contract`) and the specific `PRODUCT_VISION.md` capability (Capability 5: Agent-to-UI protocol (`A2UI`)) this deterministic action-ordering fix satisfies, with no inference required, at the current branch tip `c05dca472b10ab54a4808d75d3ba3c91ed51e4d7`.
- Canon note: the exact milestone and capability labels in this packet come from the current `ROADMAP.md` and `PRODUCT_VISION.md` checked into this worktree, so this resubmission uses the branch's present-day canon rather than older illustrative labels from the review discussion.
- Roadmap item(s) affected: `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress) -> `MVP Focus Through 2026-05-04` task anchor for `feat-a2ui-contract`; scope bullets `Define A2UI output contract for agent-produced presentation artifacts`, `Add agent-side card/section/action payload generation with deterministic schemas`, and `Provide CLI rendering fallback for the same structured payloads`; exit criterion `A2UI schema/versioning is documented and stable`
- Vision capability affected: `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`) -> agent emits structured presentation artifacts (cards, sections, actions, metadata), artifacts are consumable by CLI first, then `Exegesis Console`, then future Studio UI, and the CLI text fallback renders the same underlying artifacts
- Task anchor: `ROADMAP.md` `MVP Focus Through 2026-05-04` task anchor for `feat-a2ui-contract` sits under this milestone.

## Required Handoff Fields

- Roadmap item(s) affected: `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress)
  - Scope bullets: `Define A2UI output contract for agent-produced presentation artifacts`, `Add agent-side card/section/action payload generation with deterministic schemas`, and `Provide CLI rendering fallback for the same structured payloads`
  - Task anchor: `ROADMAP.md` `MVP Focus Through 2026-05-04` lists `feat-a2ui-contract` as a current active implementation emphasis
  - Exit criterion: `A2UI schema/versioning is documented and stable`
- Vision capability affected: `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`)
  - CLI remains able to render a text fallback of the same underlying artifacts.
  - The deterministic action-ordering fix keeps that fallback stable for the same structured artifacts.

- Audit note: these are the exact reviewer-required handoff fields, so the mapping is explicit rather than inferred from the scope goal or the runtime change description.

## Scope goal
- Canonicalize materialized A2UI action order so CLI fallback rendering stays deterministic, with matching contract assertions.

## Scope completed
- Canonicalized materialized A2UI action order in `src/qual/ui/a2ui.py` so CLI fallback rendering stays deterministic, with matching contract assertions in `tests/unit/test_a2ui_contract.py`.

## Plan alignment
- Roadmap item(s) affected: `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress)
  - Scope bullets: `Define A2UI output contract for agent-produced presentation artifacts`, `Add agent-side card/section/action payload generation with deterministic schemas`, and `Provide CLI rendering fallback for the same structured payloads`
  - Exit criterion: `A2UI schema/versioning is documented and stable`
  - This deterministic action-ordering change is the concrete payload-stability step completed by this lane because it keeps the CLI fallback rendering of the same A2UI artifacts deterministic.
- Vision capability affected: `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`)
  - Agent emits structured presentation artifacts that are consumable by CLI first, then `Exegesis Console`, then future Studio UI.
  - CLI remains able to render a text fallback of the same underlying artifacts.
  - This fix keeps that CLI text-fallback path deterministic for those same structured artifacts by canonicalizing action order before materialization.
- Roadmap task anchor: `ROADMAP.md` `MVP Focus Through 2026-05-04` task anchor for `feat-a2ui-contract`, so this lane's reviewed change is tied directly to the active Milestone 5 delivery track in the checked-in plan docs.
- Vision anchor: `PRODUCT_VISION.md` states that current MVP emphasis includes `A2UI` cards/actions that can be rendered in CLI now and `Exegesis Console` next, which is the exact contract-plus-fallback surface stabilized by this deterministic action-ordering fix.
- Audit mapping: this lane satisfies the Milestone 5 scope bullets above by keeping A2UI action payload generation deterministic and by preserving a stable CLI fallback over the same payloads, which is the direct Capability 5 linkage for this fix.
- Reviewer fix status: required fix `#1` is satisfied here because the `Roadmap item(s) affected` and `Vision capability affected` fields below explicitly name `ROADMAP.md` Milestone 5, the `MVP Focus Through 2026-05-04` task anchor, and `PRODUCT_VISION.md` Capability 5 instead of leaving them implicit.
- Source-of-truth note: reviewer examples are treated as examples only; the authoritative mapping for this branch tip is `ROADMAP.md` Milestone 5 and `PRODUCT_VISION.md` Capability 5 as quoted above.

## Reviewer required fix coverage
1. Required fix `#1`: explicitly name the `ROADMAP.md` milestone/task and `PRODUCT_VISION.md` capability this deterministic action ordering satisfies.
   Coverage: this packet now cites `ROADMAP.md` Milestone 5 scope bullets `Add agent-side card/section/action payload generation with deterministic schemas` and `Provide CLI rendering fallback for the same structured payloads`, plus `PRODUCT_VISION.md` Capability 5 `Agent-to-UI protocol (`A2UI`)` including the exact CLI text-fallback requirement.
   Audit result: reviewer re-check can confirm plan alignment directly from this packet without relying on inferred milestone/capability mapping.

## Lane/owned paths
- `src/qual/ui/**`

## Tasks completed (numbered)
1. Updated `src/qual/ui/a2ui.py` so filtered A2UI action payloads are sorted by canonical JSON before materialization.
2. Updated `tests/unit/test_a2ui_contract.py` to assert the canonical ordering behavior for materialized action payloads.
3. Updated this feature packet and `THREAD_PACKET.md` so the exact `Roadmap item(s) affected` and `Vision capability affected` fields are explicit for audit and re-review at the current branch tip.
4. Synced `.codex/packet_planner/state.json` and `tests/unit/test_packet_planner.py` so saved and re-emitted packets keep the same explicit Milestone 5 / Capability 5 mapping plus the `MVP Focus Through 2026-05-04` task anchor.
5. Resynced the handoff metadata to the current branch tip so the packet trail stays aligned with `HEAD`.

## Files changed
- `src/qual/ui/a2ui.py`
- `tests/unit/test_a2ui_contract.py`
- `tests/unit/test_packet_planner.py`
- `.codex/lane_meta/feat-a2ui-contract.json`
- `.codex/packet_planner/state.json`
- `THREAD_PACKET.md`
- `.codex/packets/lanes/feat-a2ui-contract/inbox/feature/F__codex-feat-a2ui-contract__aa875cd03ea2a8e092f527610640827baa7b7b5a__20260320T210541Z.md`

## Commands run and outcomes
- `make scope-check`: PASS
- `./quality-format.sh --check`: PASS
- `./quality-lint.sh`: PASS
- `./quality-test.sh`: PASS
- `./typecheck-test.sh`: PASS
- `make ci`: PASS

## Risks / blockers
- Risk: `LOW`
- Blockers: none

## Required handoff fields
### Roadmap item(s) affected
- `ROADMAP.md` Milestone 5: A2UI Presentation Layer (In Progress) (see lines 88-106) -> scope bullets `Add agent-side card/section/action payload generation with deterministic schemas` and `Provide CLI rendering fallback for the same structured payloads`
- Task anchor: `ROADMAP.md` `MVP Focus Through 2026-05-04` lists `feat-a2ui-contract` as a current active implementation emphasis under this milestone.
- Exit criterion: `A2UI schema/versioning is documented and stable`
- This deterministic action-ordering fix is the concrete payload-stability step that satisfies those milestone scope bullets for CLI fallback rendering.

### Vision capability affected
- `PRODUCT_VISION.md` Capability 5: Agent-to-UI protocol (`A2UI`) (see lines 44-47) -> agent emits structured presentation artifacts that are consumable by CLI first, then `Exegesis Console`, then future Studio UI, and CLI remains able to render a text fallback of the same underlying artifacts.
- `PRODUCT_VISION.md` exact fallback clause: CLI remains able to render a text fallback of the same underlying artifacts.
- Audit mapping: the same deterministic ordering change keeps the CLI text fallback stable for the structured A2UI artifacts emitted by the engine, which is the specific Capability 5 behavior this lane protects.

### Routing/provider impact note
- None

## Scope-check / ownership note
- Shared/integrator-locked edits: `NO`
- CLI fallback rendering remains preserved.
