# Feature → Review Packet

- Lane: `feat-a2ui-contract`
- Branch: `codex/feat-a2ui-contract`
- Commit: `aa875cd03ea2a8e092f527610640827baa7b7b5a`

## Scope goal
- Keep the `Milestone 5: A2UI Presentation Layer` A2UI output contract stable, preserve CLI fallback on the same structured payloads, and keep actions typed, allowlisted, and engine-authoritative.

## Lane/owned paths
- `src/qual/ui/**`

## Tasks completed (numbered)
1. Normalized direct ActionRef execution through the same typed and allowlisted contract used for card filtering before capability checks, PolicyGate evaluation, and executor dispatch.
2. Rejected malformed direct ActionRef values and passed canonicalized direct ActionRef values to the executor so engine-authoritative execution stays aligned with studio and terminal paths.
3. Preserved CLI fallback rendering for GenericCard and unknown-card payloads, including explicit text fallbacks for unsupported or malformed blocks.

## Files changed
- `.codex/lane_meta/feat-a2ui-contract.json`
- `src/qual/ui/a2ui.py`
- `tests/unit/test_a2ui_contract.py`

## Commands run and outcomes
- `make scope-check`: PASS
- `./quality-format.sh --check`: PASS
- `./quality-lint.sh`: PASS
- `./quality-test.sh`: PASS
- `./typecheck-test.sh`: PASS
- `make ci`: PASS

## Risks / blockers
- Risk: `LOW`
- Blockers: none

## Required handoff fields
### Roadmap item(s) affected
- Milestone 5: A2UI Presentation Layer - define and preserve the A2UI output contract for agent-produced presentation artifacts.
- Milestone 5: A2UI Presentation Layer - provide CLI rendering fallback for the same structured A2UI payloads.
- Milestone 5: A2UI Presentation Layer - enforce typed and allowlisted actions through the engine-authoritative PolicyGate.
### Vision capability affected
- Capability 5: Agent-to-UI protocol (A2UI) - structured cards, actions, metadata, and CLI text fallback stay on the same contract.
- Capability 4: Operator-first control surface - CLI remains a first-class fallback surface over the same engine-produced artifacts.
### Routing/provider impact note
- None

## Scope-check / ownership note
- Shared/integrator-locked edits: `NO`
