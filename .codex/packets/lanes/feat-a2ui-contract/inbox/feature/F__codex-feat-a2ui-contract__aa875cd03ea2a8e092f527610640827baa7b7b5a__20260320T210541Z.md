# Feature → Review Packet

- Lane: `feat-a2ui-contract`
- Branch: `codex/feat-a2ui-contract`
- Commit: `aa875cd03ea2a8e092f527610640827baa7b7b5a`

## Scope goal
- Keep the `Milestone 5: A2UI Presentation Layer` A2UI output contract stable, preserve CLI fallback on the same structured payloads, and keep actions typed, allowlisted, and engine-authoritative.

## Lane/owned paths
- `src/qual/ui/**`

## Tasks completed (numbered)
1. Normalized direct ActionRef execution through the same typed and allowlisted contract used for card filtering before capability checks, PolicyGate evaluation, and executor dispatch.
2. Rejected malformed direct ActionRef values and passed canonicalized direct ActionRef values to the executor so engine-authoritative execution stays aligned with studio and terminal paths.
3. Preserved CLI fallback rendering for GenericCard and unknown-card payloads, including explicit text fallbacks for unsupported or malformed blocks.

## Files changed
- `src/qual/ui/a2ui.py`
- `tests/unit/test_a2ui_contract.py`

## Budget / scope summary
- Submitted feature diff file count: `2`
- Submitted feature diff ownership: `2/2` lane-owned under `src/qual/ui/**` and tests covering that surface
- Non-owned files included in submitted feature diff: `0`
- Branch-local operational metadata after the feature commit: `4` files under `.codex/**`
- Promoted lane feature scope remains limited to the two owned source/test files above; the `.codex/**` files below are reviewer/planner transport only and are not part of the requested product-code promotion

## Commands run and outcomes
- `make scope-check`: PASS
- `./quality-format.sh --check`: PASS
- `./quality-lint.sh`: PASS
- `./quality-test.sh`: PASS
- `./typecheck-test.sh`: PASS
- `make ci`: PASS

## Risks / blockers
- Risk: `LOW`
- Blockers: none

## Required handoff fields
### Roadmap item(s) affected
- Milestone 5: A2UI Presentation Layer - define and preserve the A2UI output contract for agent-produced presentation artifacts.
- Milestone 5: A2UI Presentation Layer - provide CLI rendering fallback for the same structured payloads.
- Milestone 5: A2UI Presentation Layer - enforce typed and allowlisted actions with engine-authoritative PolicyGate behavior for direct action refs.
### Vision capability affected
- Capability 5: Agent-to-UI protocol (A2UI) - structured cards, actions, metadata, and CLI text fallback stay on the same contract.
- Capability 4: Operator-first control surface - CLI remains a first-class fallback surface over the same engine-produced artifacts.
### Routing/provider impact note
- None

## Scope-check / ownership note
- Submitted feature diff scope: lane-owned only (`src/qual/ui/a2ui.py`, `tests/unit/test_a2ui_contract.py`).
- Shared/integrator-locked edits in submitted feature diff: `NO`
- Branch-local metadata files outside lane ownership:
  - `.codex/kickoff_packets/feat-a2ui-contract.md`: kickoff packet text for planner/reviewer coordination only.
  - `.codex/lane_meta/feat-a2ui-contract.json`: lane handoff metadata backing roadmap/vision fields only.
  - `.codex/packet_planner/state.json`: planner state pointer updated by packet emission only.
  - `.codex/packets/lanes/feat-a2ui-contract/inbox/feature/F__codex-feat-a2ui-contract__aa875cd03ea2a8e092f527610640827baa7b7b5a__20260320T210541Z.md`: reviewer handoff carrier only.
- Resubmission note: this packet supersedes the earlier mixed-scope handoff and removes `.codex/**` operational files from the submitted feature file list.
- Approval/justification note: no product-code ownership exception is requested for the submitted feature diff; each `.codex/**` file above is included only because the local planner/reviewer workflow stores operational handoff state on-branch.
