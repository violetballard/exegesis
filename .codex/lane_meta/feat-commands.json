{
  "scope_goal": "Keep the feat-commands handoff aligned with the command-catalog, canonical mapping, and diff-preview CLI contract work already present on the branch, plus the two explicitly approved shared tests.",
  "review_commit": "cf6e4984d3d0f154d2be69c58f582868c9549585",
  "review_commits": [
    "cf6e4984d3d0f154d2be69c58f582868c9549585"
  ],
  "tasks_completed": [
    "Restored the command-catalog surface and added spec-aware lookup helpers plus canonical command mapping under src/qual/commands/**.",
    "Hardened diff-preview output contracts so the fingerprint covers the exact emitted payload after labels, header suppression, truncation, and summary-only handling.",
    "Added focused coverage for the command-catalog, canonical mapping, and diff-preview contract paths.",
    "Regenerated the handoff packet and lane metadata so the review evidence matches the actual code/test delta and keeps the shared-file approvals explicit."
  ],
  "risk": "HIGH",
  "roadmap_items": [
    "Milestone 3 - Product Readiness (Planned): lock the command-catalog and diff-preview CLI contracts so the operator-facing command surface stays deterministic, auditable, and ready for CLI-first use."
  ],
  "vision_capabilities": [
    "3. Auditable generation - the emitted fingerprint verifies the exact diff payload users receive, including label application and truncation behavior.",
    "4. Operator-first control surface - CLI review gates stay aligned with the command-catalog, canonical mapping, and diff-preview contract surface plus the approved shared tests."
  ],
  "routing_provider_impact": "None. This change only affects local packet metadata and command-contract test coverage; no routing/provider files change.",
  "proposed_readme_patch": "",
  "shared_file_exception": true,
  "kickoff_budget_note": "High-risk shared-test handoff with task budget 4, time budget 30m, size limits <=8 files and <=300 net LOC. The reviewed implementation spans 6 lane-owned or approved test files, including src/qual/commands/canonical.py and the two approved shared tests.",
  "approved_exception_note": "Approved shared-file exception for tests/unit/test_commands_catalog.py and tests/unit/test_diff_preview.py.",
  "files_changed": [
    { "path": "src/qual/commands/__init__.py", "kind": "code" },
    { "path": "src/qual/commands/canonical.py", "kind": "code" },
    { "path": "src/qual/commands/catalog.py", "kind": "code" },
    { "path": "src/qual/commands/diff_preview.py", "kind": "code" },
    { "path": "tests/unit/test_commands_catalog.py", "kind": "test" },
    { "path": "tests/unit/test_diff_preview.py", "kind": "test" }
  ]
}
